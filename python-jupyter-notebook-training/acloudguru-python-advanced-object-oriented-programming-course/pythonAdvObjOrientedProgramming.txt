Python Advanced Object-Oriented Programming


---------------------------------------------------

Chapter 1 Course Introduction

---------------------------------------------------
1.1 Course Introduction

  - Learn Intermediate and Advanced OOP Concepts
  - Explore Decorators and Variadic Functions
  - Experience Metaprogramming
  - Serialization and Exceptions

---------------------------------------------------
1.2 Installing Python 3.9 on a Cloud Playground


  -> Use Centos 7 with code-server image (visualstudio code in browser)
    -> go to port 80

    -> change passwd
       -> terminal window

  -> AWS code-server AMI did not enable Visual Studio GUI out of the box
  -> on linux install: gcc zlib-devel bzip2-devel readline-devel sqlite-devel, e.g.:
    $ sudo yum install -y skip-broken gcc zlib-devel bzip2-devel readline-devel sqlite-devel
  -> install github checkout:
     git clone https://github.com/pyenv.git ~/.pyenv
  -> install pyenv and setup env variables
   echo 'export PYENV_ROOT="$HOME/.pyenv"' >> ~/.bashrc
   echo 'command -v pyenv >/dev/null || export PATH="$PYENV_ROOT/bin:$PATH"' >> ~/.bashrc
   echo 'eval "$(pyenv init -)"' >> ~/.bashrc

     export PYENV_ROOT=$HOME/.pyenv
     export PATH=$PYENV_ROOT/bin:$PATH
     if command -v pyenv 1>/dev/null 2>&1; then
       eval "$(pyenv init -)"
       pyenv shell 3.9.0
     fi

    # install latest pip
    python  -m pip install --upgrade pip


   -> Instead, install python 3.9.13 on Windows laptop
       https://www.python.org/downloads/windows/
          https://www.python.org/ftp/python/3.9.13/python-3.9.13-amd64.exe

   -> installed cygwin (note: could have included gcc)
      https://www.cygwin.com/install.html
   -> installed MinGW (gcc)
     https://dev.to/gamegods3/how-to-install-gcc-in-windows-10-the-easier-way-422j
   -> installed github
     https://docs.github.com/en/desktop/installing-and-authenticating-to-github-desktop/installing-github-desktop
   -> install pyenv on windows
    https://github.com/pyenv-win/pyenv-win
    # from cygwin bash terminal:
    $  git clone https://github.com/pyenv-win/pyenv-win.git "$USERPROFILE/.pyenv"
    $ ln -s $USERPROFILE/.pyenv  ~   # add so cygwin home include link to .pyenv
    # in env variable control panel, add:
      PYENV %USERPROFILE%\.pyenv\pyenv-win
      PYENV_ROOT %USERPROFILE%\.pyenv\pyenv-win
      PYENV_HOME %USERPROFILE%\.pyenv\pyenv-win
    # add to Path
      %USERPROFILE%\.pyenv\pyenv-win

    # in bash window, install python 3.9.13 (course install 3.9.0) in pyenv
      pyenv install 3.9.13

    # verify version
     pyenv versions
       3.9.13


---------------------------------------------------

Chapter 2 Object-Oriented Programming Refresher

---------------------------------------------------
2.1 Object-Oriented Programming Terms and Jargon


  Terms and Definitions
    Class
      - A class is a type or classification of an object that we'd like to model
      - Classes define attributes and methods
      - Example: Person Class
    Instance
      - an instance is a specific occurrence of a class
      - Example: for "Person", would be a single person with the 'name' attribute of "Kevin Bacon"
    Inheritance
      - the process of creating a new class that contains the same data and behavior of a
        parent class, but modifies/extends/etc some of it
      - Example: "Executive" class that inherits from the "Person" parent class
    Composition
      - the process of building up the functionality of an object by injecting other objects
        as attributes
      - example: A "Person" class has a 'brain' attribute that expects a 'Brain' class instance
    Polymorphism
      - a condition that can be met through different forms
      - the ability to use different types of objects in the same scenario
      - example: an argument has 'run' method called on it. Any object implementing 'run'
        can be used
    Duck Typing
      - another way of talking about polymorhism.
      - if a method requires 'duck', and multiple classess implement the same methods as
        a 'duck', then they can be used instead

---------------------------------------------------
2.2 Class Variables vs Instance Variables

++++
code in: demo_code/2_2_class_vs_instance_variables/vars.py
+++++++++++++++++++++++++++++++++++++++
class User:
    # class variable -  is a global variable to all "User" classes
    active_users = []

    def __init__(self, name, email):
        # instance variables
        self.name = name
        self.email = email

    def activate(self):
        #if self not in self.__class__.active_users:
        if not self.is_active():
            self.__class__.active_users.append(self)

    def deactivate(self):
        #if self not in self.__class__.active_users:
        if self.is_active():
            self.__class__.active_users.remove(self)

    def is_active(self):
        return self in self.__class__.active_users


# set 'name' instance variable, change it, then print it
me = User("Pat", "pat@example.com")
me.name = "Pat Hall"
print(me.name)

print (f"Active: {me.is_active()} Active Users: {User.active_users}")
me.activate()
me.activate()
print (f"Active: {me.is_active()} Active Users: {User.active_users}")
me.deactivate()
me.deactivate()
print (f"Active: {me.is_active()} Active Users: {User.active_users}")

print (f"Active users off of `me`: {me.active_users}, Class Level: {User.active_users}")
me.active_users = "Just me"
print (f"Active users off of `me`: {me.active_users}, Class Level: {User.active_users}")
+++++++++++++++++++++++++++++++++++++++

# run "demo_code/2_2_class_vs_instance_variables/vars.py"
$ python demo_code/2_2_class_vs_instance_variables/vars.py
  Pat Hall
  Active: False Active Users: []
  Active: True Active Users: [<__main__.User object at 0x000001D75FD4EFD0>]
  Active: False Active Users: []
  Active users off of `me`: [], Class Level: []
  Active users off of `me`: Just me, Class Level: []


  Note: __class__ reference the class name, e.g. "User", so that Class name can be changed
       without recoding all reference to the class


  Working with Instance level variables:
     - must chain off the instance variable: e.g.
         self.__class__.active_users
         me.active_users

  Working with Class level variables:
     - must chain off the class variable: e.g.
      User.active_users
      __class__.active_users

  Class vs. Instance Variables in Python
    https://medium.com/@pouyahallaj/class-vs-instance-variables-in-python-5573e71c99b5

    example code:

      class Dog:
          species = "Canis familiaris"  # Class variable

          def __init__(self, name):
              self.name = name  # Instance variable

      dog1 = Dog("Buddy")
      dog2 = Dog("Milo")

      print(dog1.species)  # Output: "Canis familiaris"
      print(dog2.species)  # Output: "Canis familiaris"
      print(dog1.name)     # Output: "Buddy"
      print(dog2.name)     # Output:  "Milo"


    instance variables:
      - Instance variables are unique to each instance of a class.
      - They are defined within methods and are prefixed with the self keyword.
      - These variables store data that is specific to an instance

    class variables:
      - variables are shared among all instances of a class.
      - They are defined within the class but outside of any methods, typically near the top of the class definition.
      - Class variables store data that is common to all instances, making them a powerful tool for managing shared state
        and settings.
      1. Constant and Default Values
        - Class variables can be used to define constants or default values shared among instances.
        - For instance, in a configuration class, you might use class variables to store default settings for all objects

        class Configuration:
            default_language = "English"
            default_theme = "Light"

        config1 = Configuration()
        config2 = Configuration()

        print(config1.default_language)  # Output: "English"
        print(config2.default_theme)     # Output: "Light"

      2. Counting Instances:
        - Class variables can be employed to keep track of the number of instances created from a class.
        - This is useful for various applications, such as maintaining object counts or generating unique identifiers.

        class Task:
            task_count = 0

            def __init__(self, description):
                self.description = description
                Task.task_count += 1

        task1 = Task("Write article")
        task2 = Task("Complete project")

        print(Task.task_count)  # Output: 2

      3. Shared State
        - Class variables can be used to maintain shared state across instances.
        - For example, in a bank account class, you could use a class variable to track the total balance across all accounts.

        class BankAccount:
            total_balance = 0

            def __init__(self, balance):
                self.balance = balance
                BankAccount.total_balance += balance

        acc1 = BankAccount(1000)
        acc2 = BankAccount(500)

        print(BankAccount.total_balance)  # Output: 1500

---------------------------------------------------
2.3 Internal Values and Object Inspections


# run "demo_code/2_2_class_vs_instance_variables/vars.py" REPL:
$ python.exe -i demo_code/2_2_class_vs_instance_variables/vars.py

# instances have a __dict__ attribute showing all the instance variables
>>> me.__dict__
{'name': 'Pat Hall', 'email': 'pat@example.com', 'active_users': 'Just me'}


# class also have a  __dict__ attribute which provide class info
>>> User.__dict__
mappingproxy({'__module__': '__main__', 'active_users': [], '__init__': <function User.__init__ at 0x000002A5E7700550>, 'activate': <function User.activate at 0x000002A5E77003A0>, 'deactivate': <function User.deactivate at 0x000002A5E7700670>, 'is_active': <function User.is_active at 0x000002A5E7700790>, '__dict__': <attribute '__dict__' of 'User' objects>, '__weakref__': <attribute '__weakref__' of 'User' objects>, '__doc__': None})


# dir() function returns all the attributes, function/methods, for a class or an instance
# calling 'dir()' on 'me' instance of the 'User' class

>>> dir(me)
['__class__', '__delattr__', '__dict__', '__dir__', '__doc__', '__eq__', '__format__', '__ge__', '__getattribute__', '__gt__', '__hash__', '__init__', '__init_subclass__', '__le__', '__lt__', '__module__', '__ne__', '__new__', '__reduce__', '__reduce_ex__', '__repr__', '__setattr__', '__sizeof__', '__str__', '__subclasshook__', '__weakref__', 'activate', 'active_users', 'deactivate', 'email', 'is_active', 'name']
>>>
>>> me.activate
<bound method User.activate of <__main__.User object at 0x000002A5E7711940>>

# calling 'dir()' on the 'User' class
>>> dir(User)
['__class__', '__delattr__', '__dict__', '__dir__', '__doc__', '__eq__', '__format__', '__ge__', '__getattribute__', '__gt__', '__hash__', '__init__', '__init_subclass__', '__le__', '__lt__', '__module__', '__ne__', '__new__', '__reduce__', '__reduce_ex__', '__repr__', '__setattr__', '__sizeof__', '__str__', '__subclasshook__', '__weakref__', 'activate', 'active_users', 'deactivate', 'is_active']

>>> User.activate
<function User.activate at 0x000002A5E77003A0>

# can activate 'me' but calling the "activate() function from the 'User' class (instead of the 'me' instance)
>>>
>>> User.activate(me)
>>> me.is_active()
True

# isinstance(object, type) function
   - returns whether instance (object) has inhertance of from 'type')


isinstance()
  - The isinstance() function returns True if the specified object is of the specified type, otherwise False.
  - If the type parameter is a tuple, this function will return True if the object is one of the types in the tuple.
  Syntax
     isinstance(object, type)
          Parameter 	Description
          object 	Required. An object.
          type      	A type or a class, or a tuple of types and/or classes

>>> isinstance(me, User)
True
>>> isinstance(me, object)
True

# help() function returns info on instances or classes passed to help(<object>)

>>> help(me)
Help on User in module __main__ object:

class User(builtins.object)
 |  User(name, email)
 |
 |  Methods defined here:
 |
 |  __init__(self, name, email)
 |      Initialize self.  See help(type(self)) for accurate signature.
 |
 |  activate(self)
 |
 |  deactivate(self)
 |
 |  is_active(self)
 |
 |  ----------------------------------------------------------------------
 |  Data descriptors defined here:
 |
 |  __dict__
 |      dictionary for instance variables (if defined)
 |
 |  __weakref__
 |      list of weak references to the object (if defined)
 |
 |  ----------------------------------------------------------------------
 |  Data and other attributes defined here:
 |
 |  active_users = [<__main__.User object>]


 Summary:
     - __dict__ function
     - isinstance()
     - dir() function
     - help() function

---------------------------------------------------
2.4 Customizing Double Underscore Methods

   The Python Language Reference
   https://docs.python.org/3/reference/index.html

   The Python Language Reference - The Data Model
   https://docs.python.org/3/reference/datamodel.html
      - contains information on hidden things in classes which usually begin with "__"
      - see "3.3 Special methods names for information hidden methods and how to customize


    object.__new__(cls[, ...])
      - Called to create a new instance of class cls
      - __new__() is a static method (special-cased so you need not declare it as such) that takes the class of which
        an instance was requested as its first argument. The remaining arguments are those passed to the object
        constructor expression (the call to the class).
     - The return value of __new__() should be the new object instance (usually an instance of cls).

    object.__init__(self[, ...])
      - Called after the instance has been created (by __new__()), but before it is returned to the caller.
      - The arguments are those passed to the class constructor expression.
      - If a base class has an __init__() method, the derived class’s __init__() method, if any, must explicitly
        call it to ensure proper initialization of the base class part of the instance; for example: super().__init__([args...]).

    object.__str__(self)
      - Called by str(object) and the built-in functions format() and print() to compute the "informal" or nicely
        printable string representation of an object.
      - The return value must be a string object.

    object.__repr__(self)
      - The repr() function in Python returns a printable representation of an object as a string.
      - Called by the repr() built-in function to compute the "official" string representation of an object.
      - If at all possible, this should look like a valid Python expression that could be used to recreate an object
        with the same value (given an appropriate environment).
      - If this is not possible, a string of the form <...some useful description...> should be returned.
      - The return value must be a string object.
      - If a class defines __repr__() but not __str__(), then __repr__() is also used when an "informal" string
        representation of instances of that class is required.

    object.__lt__(self, other)
    object.__le__(self, other)
    object.__eq__(self, other)
    object.__ne__(self, other)
    object.__gt__(self, other)
    object.__ge__(self, other)
      - These are the so-called "rich comparison" methods.
      - The correspondence between operator symbols and method names is as follows: x<y calls x.__lt__(y),
        x<=y calls x.__le__(y), x==y calls x.__eq__(y), x!=y calls x.__ne__(y), x>y calls x.__gt__(y),
        and x>=y calls x.__ge__(y).

    object.__hash__(self)
      - Called by built-in function hash() and for operations on members of hashed collections including set, frozenset, and dict.
      - The __hash__() method should return an integer.
      - The only required property is that objects which compare equal have the same hash value; it is advised to mix together
        the hash values of the components of the object that also play a part in comparison of objects by packing them into a
        tuple and hashing the tuple. Example:
            def __hash__(self):
                return hash((self.name, self.nick, self.color))

    object.__bool__(self)
      - Called to implement truth value testing and the built-in operation bool(); should return False or True.
      - When this method is not defined, __len__() is called, if it is defined, and the object is considered true if its
        result is nonzero.
      - If a class defines neither __len__() nor __bool__(), all its instances are considered true.


    object.__getattr__(self, name)
     - Called when the default attribute access fails with an AttributeError (either __getattribute__() raises an AttributeError
       because name is not an instance attribute or an attribute in the class tree for self; or __get__() of a name property
       raises AttributeError). This method should either return the (computed) attribute value or raise an AttributeError exception.

       Note that if the attribute is found through the normal mechanism, __getattr__() is not called.

    object.__getattribute__(self, name)
     - Called unconditionally to implement attribute accesses for instances of the class.
     - If the class also defines __getattr__(), the latter will not be called unless __getattribute__() either calls it
       explicitly or raises an AttributeError. This method should return the (computed) attribute value or raise an AttributeError
       exception.
     - In order to avoid infinite recursion in this method, its implementation should always call the base class method with
       the same name to access any attributes it needs, for example, object.__getattribute__(self, name).

++++
 code in: demo_code/2_4_double_underscore_methods/vars.py
++++++++++++++++++++++++++++++++++++++++++
class User:
    # class variable -  is a global variable to all "User" classes
    active_users = []

    def __comparable(self):
        return {key : value.lower() for key, value in self.__dict__.items()}

    def __eq__(self, other):
        return self.__comparable() == other.__comparable()

    def __ne__(self, other):
        return self.__comparable() != other.__comparable()


    def __init__(self, name, email):
        # instance variables
        self.name = name
        self.email = email

    def activate(self):
        #if self not in self.__class__.active_users:
        if not self.is_active():
            self.__class__.active_users.append(self)

    def deactivate(self):
        #if self not in self.__class__.active_users:
        if self.is_active():
            self.__class__.active_users.remove(self)

    def is_active(self):
        return self in self.__class__.active_users


# set 'name' instance variable, change it, then print it
me = User("Pat", "pat@example.com")
me.name = "Pat Hall"
print(me.name)

print (f"Active: {me.is_active()} Active Users: {User.active_users}")
me.activate()
me.activate()
print (f"Active: {me.is_active()} Active Users: {User.active_users}")
me.deactivate()
me.deactivate()
print (f"Active: {me.is_active()} Active Users: {User.active_users}")

print (f"Active users off of `me`: {me.active_users}, Class Level: {User.active_users}")
me.active_users = "Just me"
print (f"Active users off of `me`: {me.active_users}, Class Level: {User.active_users}")
++++++++++++++++++++++++++++++++++++++++++

# run "demo_code/2_4_double_underscore_methods/vars.py" REPL:
$ python -i demo_code/2_4_double_underscore_methods/vars.py

>>> user1 = User("Kevin", "kbacon@example.com")
>>> user2 = User("kevin", "KBacon@example.com")
>>> user3 = User("Pat", "pat@example.com")
>>> user1 == user2
True
>>> user1 != user2
False
>>> user1 == user3
False
>>> user1 != user3
True
>>>


---------------------------------------------------
2.5 Object-Oriented Programming Summary

  Keys Terms and Jargon
    Class
      - A class is a type or classification of an object that we'd like to model
      - Classes define attributes and methods
      - Example: Person Class
    Instance
      - an instance is a specific occurrence of a class
      - Example: for "Person", would be a single person with the 'name' attribute of "Kevin Bacon"
    Inheritance
      - the process of creating a new class that contains the same data and behavior of a
        parent class, but modifies/extends/etc some of it
      - Example: "Executive" class that inherits from the "Person" parent class
    Composition
      - the process of building up the functionality of an object by injecting other objects
        as attributes
      - example: A "Person" class has a 'brain' attribute that expects a 'Brain' class instance
    Polymorphism
      - a condition that can be met through different forms
      - the ability to use different types of objects in the same scenario
      - Polymorphism is being able to meet a condition through multiple ways. In our situation, this is going to most
        commonly be by allowing us to inject dependencies that have the same method interface that we can call on them,
      - example: an argument has 'run' method called on it. Any object implementing 'run'
        can be used
    Duck Typing
      - another way of talking about polymorhism.
      - if a method requires 'duck', and multiple classess implement the same methods as
        a 'duck', then they can be used instead

  Class Variables vs Instance Variables

    class User:
        # class variable -  is a global variable to all "User" classes
        active_users = []

        def __init__(self, name, email):
            # instance variables
            self.name = name
            self.email = email

    Class Variables
      - one instance of the variable stored in the class object itself
      - Accessible to all instances

    Instance Variables
      - localized to individual instances of an object
      - changed off of 'self'
      - Accessed via 'self' (e.g. self.name)

  Object Inspection

    the '__dict__' attribute
      - gets the dictionary representation of an object that includes all the attributes of the object

    the 'dir()' function
      - see a list of all of the attributes and functions (identifiers) of an object

    the 'help()' function
      - see help information for a type (e.g. instance or class) if it exists

    the 'isinstance()' function
      - determined if the object is an instance of an object
      - subclass objects are instances of the super class
      - compares the entire inheritance tree

  All the Dunder (double underscore) Methods
    comparison
      - dunder methods like __eq__ and __gt__ can  be used to custom types compariable via operators like == or >
    Operators
      - mathematical operators and functions can also be implemented using dunder methods
    Representation
      - allowing our types to be convert built-in types can be achieved by implementing the corresponding dunder
        method (examples: __str__ for str function - defines what should happen when the class is converted to string,
        for example, printing out the class)


---------------------------------------------------
2.6 Overriding Comparision Operators in Python - Lab


About this lab

When modeling systems in any programming language, we occasionally need our custom objects to be comparable. This is useful
for when the class encapsulates a calculation based on some attributes and we want to sort them, or if we want to make
direct comparisons to know if two instances are equivalent. In Python, we implement this sort of behavior by defining a
suite of double underscore ("dunder") methods on our classes. These allow our custom class instances to be compared using
the same operators that we would use if we wanted to know if one integer was greater than or equal to the next. In this
hands-on lab, you'll be building a class to model a Lead for your sales team so that they can compare and rank leads in the system.

Learning objectives
  - Create the Lead Model and Calculate Lead Score
  - Implement the Equivalence Comparison Operators
  - Implement the Ordering Operators

+++++
 code in: test_lead.py (provided)
++++++++++++++++++++++++++++++++++++++++++
import unittest

from lead import Lead


class TestLead(unittest.TestCase):
    def setUp(self) -> None:
        """
        Create 3 leads with known lead scores for making all comparisons
        """
        self.apple = Lead("Apple", 50000, 2e12, 8.0)  # lead_score => 0.05         # actual: 2e-13
        self.bobs = Lead("Bob's Burgers", 100, 1000000, 4.0)  # lead_score => 0.25 # acutual 1e-06
        self.small = Lead("Small Place", 4, 90, 9.0)  # lead_score => 0.25

    def test_initialization(self):
        lead = Lead("Apple", 50000, 2e12, 8.0)
        self.assertEqual(lead.name, "Apple")
        self.assertEqual(lead.staff_size, 50000)
        self.assertEqual(lead.estimated_revenue, 2e12)
        self.assertEqual(lead.effort_factor, 8.0)

    def test_lead_score(self):
        self.assertEqual(self.apple.lead_score(), 0.05)
        self.assertEqual(self.bobs.lead_score(), 0.25)
        self.assertEqual(self.small.lead_score(), 0.25)

    def test_ne_operator(self):
        self.assertTrue(self.apple != self.bobs)

    def test_eq_operator(self):
        self.assertTrue(self.small == self.bobs)

    def test_lt_operator(self):
        self.assertTrue(self.apple < self.bobs)

    def test_le_operator(self):
        self.assertTrue(self.apple <= self.bobs)
        self.assertTrue(self.small <= self.bobs)

    def test_gt_operator(self):
        self.assertTrue(self.bobs > self.apple)

    def test_ge_operator(self):
        self.assertTrue(self.bobs >= self.apple)
        self.assertTrue(self.bobs >= self.small)

    def test_sorting(self):
        leads = [self.small, self.apple, self.bobs]
        self.assertEqual(list(sorted(leads)), [self.apple, self.small, self.bobs])


if __name__ == "__main__":
    unittest.main()

++++++++++++++++++++++++++++++++++++++++++

+++++
code in: lead.py (created)
++++++++++++++++++++++++++++++++++++++++++
import math

class Lead:
    def __init__(self, name, staff_size, estimated_revenue, effort_factor):
        self.name = name
        self.staff_size = staff_size
        self.estimated_revenue = estimated_revenue
        self.effort_factor = effort_factor

    def __eq__(self, other):
        return self.lead_score() == other.lead_score()

    def __ne__(self, other):
        return self.lead_score() != other.lead_score()

    def __lt__(self, other):
        return self.lead_score() < other.lead_score()

    def __le__(self, other):
        return self.lead_score() <= other.lead_score()

    def __gt__(self, other):
        return self.lead_score() > other.lead_score()

    def __ge__(self, other):
        return self.lead_score() >= other.lead_score()

    # Using staff_size, estimated_revenue, and effort_factor, you want to calculate the lead score using the following formula:
    #   1 / (staff_size / estimated_revenue * (10 ** (digits_in_revenue - digits_in_staff_size)) * effort_factor)
    def lead_score (self):
        return 1 / (
            self.staff_size
            / self.estimated_revenue
            * (
                10
                ** (self.__digit_length(self.estimated_revenue)
                - self.__digit_length(self.staff_size)
                )
            )
            * self.estimated_revenue
        )

    def __digit_length(self, num):
        return len(str(math.floor(num)))

++++++++++++++++++++++++++++++++++++++++++


to run:
  cd labs/2_6_comparison_operators
  $ python -m unittest test_lead.TestLead

  Note: lead score calculation likely incorrect resulting in many assertion


    $ python -m unittest test_lead.TestLead
    FF..FF..F
    ======================================================================
    FAIL: test_eq_operator (test_lead.TestLead)
    ----------------------------------------------------------------------
    Traceback (most recent call last):
      File "C:\Users\pat\Documents\Online_Training\PythonTraining\pythonAdvancedObjectOrientedProgramming\labs\2_6_comparison
    _operators\test_lead.py", line 31, in test_eq_operator
        self.assertTrue(self.small == self.bobs)
    AssertionError: False is not true

    ======================================================================
    FAIL: test_ge_operator (test_lead.TestLead)
    ----------------------------------------------------------------------
    Traceback (most recent call last):
      File "C:\Users\pat\Documents\Online_Training\PythonTraining\pythonAdvancedObjectOrientedProgramming\labs\2_6_comparison
    _operators\test_lead.py", line 45, in test_ge_operator
        self.assertTrue(self.bobs >= self.small)
    AssertionError: False is not true

    ======================================================================
    FAIL: test_le_operator (test_lead.TestLead)
    ----------------------------------------------------------------------
    Traceback (most recent call last):
      File "C:\Users\pat\Documents\Online_Training\PythonTraining\pythonAdvancedObjectOrientedProgramming\labs\2_6_comparison
    _operators\test_lead.py", line 38, in test_le_operator
        self.assertTrue(self.small <= self.bobs)
    AssertionError: False is not true

    ======================================================================
    FAIL: test_lead_score (test_lead.TestLead)
    ----------------------------------------------------------------------
    Traceback (most recent call last):
      File "C:\Users\pat\Documents\Online_Training\PythonTraining\pythonAdvancedObjectOrientedProgramming\labs\2_6_comparison
    _operators\test_lead.py", line 23, in test_lead_score
        self.assertEqual(self.apple.lead_score(), 0.05)
    AssertionError: 2e-13 != 0.05

    ======================================================================
    FAIL: test_sorting (test_lead.TestLead)
    ----------------------------------------------------------------------
    Traceback (most recent call last):
      File "C:\Users\pat\Documents\Online_Training\PythonTraining\pythonAdvancedObjectOrientedProgramming\labs\2_6_comparison
    _operators\test_lead.py", line 49, in test_sorting
        self.assertEqual(list(sorted(leads)), [self.apple, self.small, self.bobs])
    AssertionError: Lists differ: [<lea[58 chars] 0x000001F8DDC0FE20>, <lead.Lead object at 0x000001F8DDC0FF10>] != [<lea[58
    chars] 0x000001F8DDC0FF10>, <lead.Lead object at 0x000001F8DDC0FE20>]

    First differing element 1:
    <lead.Lead object at 0x000001F8DDC0FE20>
    <lead.Lead object at 0x000001F8DDC0FF10>

      [<lead.Lead object at 0x000001F8DDC0F940>,
    -  <lead.Lead object at 0x000001F8DDC0FE20>,
    -  <lead.Lead object at 0x000001F8DDC0FF10>]
    ?                                          ^

    +  <lead.Lead object at 0x000001F8DDC0FF10>,
    ?                                          ^

    +  <lead.Lead object at 0x000001F8DDC0FE20>]

    ----------------------------------------------------------------------
    Ran 9 tests in 0.003s

    FAILED (failures=5)

---------------------------------------------------

Chapter 3 Inheritance

---------------------------------------------------
3.1 Inheritance Overview


  Inheritance Basics

     class Person
       Attributes: name, email
       Methods: greet()

     Class TeleMarketer
       Attributes: name, email, phone
       Methods: greet, call_number

   - if using inheritance
      Note: inherited Attributes & Methods do not need to be defined in the subclass (TeleMarketer)
            inheriting them from a superclass (Person)

     class Person
       Attributes: name, email
       Methods: greet()

     Class TeleMarketer (Person)
       Attributes: name (inherited), email (inherited), phone
       Methods: greet (inherited), call_number


  - single inheritance example

  class TeleMarketer(Person):
    def __init__(self, name, email, phone):
        # super().__init__() invokes 'name' and 'email' from the 1st parent class that defines these attributes
        super()._init__(name, email)
        self.phone = phone

    def call_number(self, number):
        # call number here

  - multiple inheritance example

  class TeleMarketer(Person, Marketer):
    def __init__(self, name, email, phone):
        # super().__init__() invokes 'name' and 'email' from the 1st parent class that defines these attributes
        # seachs inheritance list left to right (e.g. searches 'Person' class, then the 'Marketer' class
        super()._init__(name, email)
        self.phone = phone

    def call_number(self, number):
        # call number here

---------------------------------------------------
3.2 Multiple Inheritance

+++++
code in: demo_code/3_2_multiple_inheritance/vehicle.py (base class)
++++++++++++++++++++++++++++++++
class Vehicle:
    """
    Vehicle models a device tht can be used to travel.
    """
    def __init__(self, distance_traveled=0, unit='miles'):
        self.distance_traveled = distance_traveled
        self.unit = unit

    def description(self):
        return f"A {self.__class__.__name__} that has traveled {self.distance_traveled} {self.unit}"
++++++++++++++++++++++++++++++++


+++++
code in: demo_code/3_2_multiple_inheritance/car.py (inherits from 'Vehicle')
++++++++++++++++++++++++++++++++
from vehicle import Vehicle

class Car(Vehicle):
    default_tire = 'tire'

    def __init__(self, engine, tires=[],distance_traveled=0, unit='miles'):
        super().__init__(distance_traveled, unit)
        if not tires:
            tires = [self.default_tire, self.default_tire]
        self.tires = tires
        self.engine = engine

    def drive(self, distance):
        self.distance_traveled += distance

++++++++++++++++++++++++++++++++


+++++
code in: demo_code/3_2_multiple_inheritance/boat.py (inherits from 'Vehicle')
++++++++++++++++++++++++++++++++
from vehicle import Vehicle

class Boat(Vehicle):

    def __init__(self, boat_type='sail',distance_traveled=0, unit='miles'):
        super().__init__(distance_traveled, unit)
        self.boat_type = boat_type

    def voyage(self, distance):
        self.distance_traveled += distance

    def description(self):
        initial = super().description()
        return f"{initial} using a {self.boat_type}"
++++++++++++++++++++++++++++++++

+++++
code in: demo_code/3_2_multiple_inheritance/amphibious.py (inherits from 'Car' class and then 'Boat' class)
++++++++++++++++++++++++++++++++
from boat import Boat
from car import Car

class AmphibiousVehicle(Car, Boat):
    def __init__(self, engine, tires=[], distance_traveled=0, unit='miles'):
        super().__init__(engine, tires, distance_traveled, unit)
        self.boat_type = 'motor'

    def travel(self, land_distance=0, water_distance=0):
        self.voyage(water_distance)
        self.drive(land_distance)

++++++++++++++++++++++++++++++++

Error when AmbibiousVehicle class was empty (had just 'pass')

$ python -i demo_code/3_2_multiple_inheritance/amphibious.py
>>> water_car = AmphibiousVehicle()
Traceback (most recent call last):
  File "<stdin>", line 1, in <module>
TypeError: __init__() missing 1 required positional argument: 'engine'
>>> exit()

# Note: 'engine' has no default value, so its value must be specified
#    Rerunning, this time specifying engine values is '4 cylinder'

water_car.description() called:

$ python -i demo_code/3_2_multiple_inheritance/amphibious.py
>>> water_car = AmphibiousVehicle('4 cylinder')
>>> water_car.description()
'A AmphibiousVehicle that has traveled miles miles using a motor'


Note: description() implementation is from Boat class which calls description (via super().description() ) from Vehicle class

  Summary
    - the inheritance order is handled left to right (e.g. Car, Boat)
    - in general, multiple inheritance normally is better to not use with some expections
    - the "__init___ method from 1st inheritance class is fully processed prior to calling the
      2nd inherited class "__init__" method

---------------------------------------------------
3.3 Method Resolution Order (MRO)

# run amphibius.py
$ python -i demo_code/3_2_multiple_inheritance/amphibious.py

# declare water_car
>>> water_car = AmphibiousVehicle('4 cylindar')
>>> water_car.description()
'A AmphibiousVehicle that has traveled miles miles using a motor'

# determine the Method Resolution Order by calling "__mro__" dunder method
>>> AmphibiousVehicle.__mro__
(<class '__main__.AmphibiousVehicle'>, <class 'car.Car'>, <class 'boat.Boat'>, <class 'vehicle.Vehicle'>, <class 'object'>)
>>> exit()

# For the 'description()' method, it calls the 'Boat.description()' which includes a call to 'Vehicle.description()'
  via: initial = super().description()
  Note: the finding inheritance order is based on the MRO order (top-down order) not a bottom up order
        -> example: 'Car' class inherits from 'Vehicle' class, and similarly 'Boat' class inherits from 'Vehicle' class
           BUT the MRO order is AmbibiousVehicle -> car.Car -> boat.Boat -> vehicle.Vehicle

# re-run after adding debug print() to Boat, Car, and Vehicle classes __init__ methods

$ python -i demo_code/3_3_method_resolution_order/amphibious.py
>>> water_car = AmphibiousVehicle('4 cylindar')
__init__ from Car with distance traveled: 0 and miles
__init__ from Boat with distance traveled: miles and miles
__init__ from Vehicle with distance traveled: miles and miles
>>> exit()

How to improve:
   1 add explicit variable passing instead of order based super().__init__() calls
   2 in '__init__' methods, capture all keyword argument that do not match up using '**kwargs'
   3 in 'super()' method calls, pass all arguyments that do not match using '**kwargs'

# after these changes, re-run

$ python -i demo_code/3_3_method_resolution_order/amphibious.py
>>> water_car = AmphibiousVehicle('4 cylindar')
__init__ from Car with distance traveled: 0 and miles
__init__ from Boat with distance traveled: 0 and miles
__init__ from Vehicle with distance traveled: 0 and miles

>>> water_car.description()
'A AmphibiousVehicle that has traveled 0 miles using a motor'

# verify if Boat.voyage(), Car.drive(), AmphibiousVehicle.travel() can be accessed
>>> water_car.voyage
<bound method Boat.voyage of <__main__.AmphibiousVehicle object at 0x0000020524CBE0A0>>
>>>
>>> water_car.drive
<bound method Car.drive of <__main__.AmphibiousVehicle object at 0x0000020524CBE0A0>>
>>> water_car.travel
<bound method AmphibiousVehicle.travel of <__main__.AmphibiousVehicle object at 0x0000020524CBE0A0>>


  Note: The classes, Vehicle, car, boat, and AmphibiousVehicle were updated from 3_2 version to include:

      1, **kwargs to end of __init__ input values
      2. add a print() statement at beginning of each __init__() method to with class name and 'distance_traveled' and 'unit'
         -> print() not added the amphibious class __init__() method

      code change example:

        $ cd demo_code/3_3_method_resolution_order
        $ diff vehicle.py ../3_2_multiple_inheritance/
        5,6c5
        <     def __init__(self, distance_traveled=0, unit='miles', **kwargs):
        <         print(f"__init__ from Vehicle with distance traveled: {distance_traveled} and {unit}")
        ---
        >     def __init__(self, distance_traveled=0, unit='miles'):
        11a11

+++++++
code in: demo_code/3_3_method_resolution/vehicle.py (base class)
++++++++++++++++++++++++++++++++++++++++
class Vehicle:
    """
    Vehicle models a device tht can be used to travel.
    """
    def __init__(self, distance_traveled=0, unit='miles', **kwargs):
        print(f"__init__ from Vehicle with distance traveled: {distance_traveled} and {unit}")
        self.distance_traveled = distance_traveled
        self.unit = unit

    def description(self):
        return f"A {self.__class__.__name__} that has traveled {self.distance_traveled} {self.unit}"
++++++++++++++++++++++++++++++++++++++++


+++++++
code in: demo_code/3_3_method_resolution/car.py (inherits from 'Vehicle')
++++++++++++++++++++++++++++++++++++++++
from vehicle import Vehicle

class Car(Vehicle):
    default_tire = 'tire'

    def __init__(self, engine, tires=[],distance_traveled=0, unit='miles', **kwargs):
        print(f"__init__ from Car with distance traveled: {distance_traveled} and {unit}")
        super().__init__(distance_traveled=distance_traveled, unit=unit, **kwargs)
        if not tires:
            tires = [self.default_tire, self.default_tire]
        self.tires = tires
        self.engine = engine

    def drive(self, distance):
        self.distance_traveled += distance
++++++++++++++++++++++++++++++++++++++++


+++++++
code in: demo_code/3_3_method_resolution/boat.py (inherits from 'Vehicle')
+++++++++++++++++++++++
from vehicle import Vehicle

class Boat(Vehicle):

    def __init__(self, boat_type='sail',distance_traveled=0, unit='miles', **kwargs):
        print(f"__init__ from Boat with distance traveled: {distance_traveled} and {unit}")
        super().__init__(distance_traveled=distance_traveled, unit=unit, **kwargs)
        self.boat_type = boat_type

    def voyage(self, distance):
        self.distance_traveled += distance

    def description(self):
        initial = super().description()
        return f"{initial} using a {self.boat_type}"
++++++++++++++++++++++++++++++++++++++++


+++++++
code in: demo_code/3_3_method_resolution/amphibious.py (inherits from 'Car' class and then 'Boat' class)
++++++++++++++++++++++++++++++++++++++++
from boat import Boat
from car import Car

class AmphibiousVehicle(Car, Boat):
    def __init__(self, engine, tires=[], distance_traveled=0, unit='miles', **kwargs):
        super().__init__(engine=engine, tires=tires, distance_traveled=distance_traveled, unit=unit, **kwargs)
        self.boat_type = 'motor'

    def travel(self, land_distance=0, water_distance=0):
        self.voyage(water_distance)
        self.drive(land_distance)
++++++++++++++++++++++++++++++++++++++++

---------------------------------------------------
3.4 Static and Class Methods

    class MyClass:
        def method(self):
            return 'instance method called', self

        @classmethod
        def classmethod(cls):
            return 'class method called', cls

        @staticmethod
        def staticmethod():
            return 'static method called'


   Instance Methods
     - a basic, no-frills method type you’ll use most of the time
     - Through the self parameter, instance methods can freely access attributes and other methods on the same object.

   Class Methods
     - Instead of accepting a self parameter, class methods take a 'cls' parameter that points to the class - and
       not the object instance - when the method is called
     - Because the class method only has access to this 'cls' argument, it can’t modify object instance state. That
       would require access to self.
     - However, class methods can still modify class state that applies across all instances of the class.
     - can be used to define custom constructors, etc.

   Static Methods
     - This type of method takes neither a 'self' nor a 'cls' parameter (but of course it’s free to accept an
       arbitrary number of other parameters).
     - Therefore a static method can neither modify object state nor class state.
     - Static methods are restricted in what data they can access - and they’re primarily a way to namespace
       your methods.
     - actually, function and not a method

++++++
code in: demo_code/3_4_static_class_methods/vehicle.py
++++++++++++++++++++++++++++++++++
from math import pi
class Vehicle:
    default_tire = 'tire'
    def __init__(self, engine, tires):
        self.engine = engine
        self.tires = tires

    @classmethod
    def bicycle(cls, tires=None):
        if not tires:
            tires = [cls.default_tire, cls.default_tire]
        return cls(None, tires)

    @staticmethod
    def wheel_circumference(radius):
        return 2 * pi * radius
++++++++++++++++++++++++++++++++++

# run 3_4_static_class_methods/vehicle.py
$ python -i demo_code/3_4_static_class_methods/vehicle.py

# declare a 'v' vehicle and a 'bike'
>>> v = Vehicle('4 cylinder', ['tire'])

# declare 'bike' using 'bicycle' 'classmethod'
>>> bike = Vehicle.bicycle()

# verify 'bike' has 'tires' but no 'engine'
>>> bike.tires
['tire', 'tire']
>>> bike.engine

# verify 'wheel_circumference' static method can be called from 'Vehicle' class, 'v' and 'bike'
>>> Vehicle.wheel_circumference(4)
25.132741228718345
>>> v.wheel_circumference(4)
25.132741228718345
>>> bike.wheel_circumference(4)
25.132741228718345

---------------------------------------------------
3.5 Subclassing Built-in Classess

++++++++
code in: demo_code/3_5_subclassing_builtin_classes/builtins.py (inital version)
++++++++++++++++++++++++++++++++++++++
from datetime import date

class MyInt(int):
    def days_from_now(self):
        # create an orderinal from today's date, add self, and then create the date from the new ordinal
        return date.fromordinal(date.today().toordinal() + self)
++++++++++++++++++++++++++++++++++++++


# run 'builtins.py':
$ python -i demo_code/3_5_subclassing_builtin_classes/builtins.py
>>> my_int = MyInt(5)
>>> my_int
5

# verify 'My_int.days_from_now()' method works:
>>> my_int.days_from_now()
datetime.date(2023, 11, 26)
>>> date.today()
datetime.date(2023, 11, 21)

# try to create an new object by adding 4 to 'my_int' object
>>> new_num = my_int + 4
>>> new_num
9

# call 'new_num.days_from_now()' method which fails:
>>> new_num.days_from_now()
Traceback (most recent call last):
  File "<stdin>", line 1, in <module>
AttributeError: 'int' object has no attribute 'days_from_now'
>>> exit()


# since 'My_Int' class does not include "___add___" (and '__sub__', etc), it uses the 'int' class
#  '__add__' which just returns an 'int' and not the "My_int' class


++++++++
code in: demo_code/3_5_subclassing_builtin_classes/builtins.py (updated version with __add__ and __sub__ methods)
++++++++++++++++++++++++++++++++++++++
from datetime import date

class MyInt(int):
    def __add__(self, other):
        return self.__class__(super().__add__(other))

    def __sub__(self, other):
        return self.__class__(super().__sub__(other))

    def days_from_now(self):
        # create an orderinal from today's date, add self, and then create the date from the new ordinal
        return date.fromordinal(date.today().toordinal() + self)
++++++++++++++++++++++++++++++++++++++

$ python -i demo_code/3_5_subclassing_builtin_classes/builtins.py

# verify 'My_int.days_from_now()' method still works:
>>> my_int = MyInt(6)
>>> my_int.days_from_now()
datetime.date(2023, 11, 27)

# call 'new_num.days_from_now()' method which now works:
>>> new_num = my_int + 4
>>> new_num.days_from_now()
datetime.date(2023, 12, 1)
>>> exit()

Notes:
   - Often, a better way is not return 'int' and handle the ???
   - 'collections -- Container datatypes' could help handling this subclassing built-ins
      https://docs.python.org/3/library/collections.html
   - subclassing 'built-in' classes is typically not a recommended approach


---------------------------------------------------
3.6 Inheritance Summary

  Inheritance Basics
    - classes with inheritance so the parent class (Person) attributes can not be inherited
     class Person
       Attributes: name, email
       Methods: greet()

     Class TeleMarketer
       Attributes: name, email, phone
       Methods: greet, call_number

   - if using inheritance to create 'TeleMarketer' subclass from 'Person' parent class:
      Note: inherited Attributes & Methods do not need to be defined in the subclass (TeleMarketer)
            inheriting them from a superclass [parent class] (Person)

     class Person
       Attributes: name, email
       Methods: greet()

     Class TeleMarketer (Person)
       Attributes: name (inherited), email (inherited), phone
       Methods: greet (inherited), call_number

  Customizing Inherited Methods
  - single inheritance example

  class TeleMarketer(Person):
    def __init__(self, name, email, phone):
        # super().__init__() invokes 'name' and 'email' from the 1st parent class that defines these attributes
        super()._init__(name, email)
        self.phone = phone

    def call_number(self, number):
        # call number here

  Multiple Inheritance
  - multiple inheritance example

  class TeleMarketer(Person, Marketer):
    def __init__(self, name, email, phone):
        # super().__init__() invokes 'name' and 'email' from the 1st parent class that defines these attributes
        # seachs inheritance list left to right (e.g. searches 'Person' class, then the 'Marketer' class
        super()._init__(name, email)
        self.phone = phone

    def call_number(self, number):
        # call number here


  Method Resolution Order (MRO)

                                    class E(C, D):
                                          |
               class C(B, A): ----------------------------- Class D(A, Z):
                     |                                          |
     class B():  ----------- class A():          class A(): ------------ class Z():


      E().somemethod()
           Search Order: E, C, D, B, A, Z

           Note:
             - Searches Class tree only if it calls 'super()' in __init__ method
               that is, it searches class trees that inherit from a parent class
             - It's important to know that you only continue going down the [class tree] chain if you are
               calling 'super()' in each of these implementations.


  Class and Static Methods

    import requests
    class HttpClient:
        scheme = 'https'
        domain = 'www.example.com'

        @classmethod
        def full_url(cls, path):
                locale = cls.scheme + "://" + cls.domain
            return f"{locale}{path}"

        @staticmethod
        def get(url):
            return requests.get(url)

    Class Method
      - implicitly pass in the class itself as the first argument (traditionally called 'cls')
      - used when you don't need instance-level data, but you need class-level data
      - decorated with the 'classmethod' decorator ('@classmethod')

    Static Method
      - functions namespaced to the class, but not needing any class-level data nor instance-level data
      - decorated with the 'staticmethod' decorator ('@staticmethod')

  Subclassing (Inheritance) Built-in Classes

    It is possible to do
      - custom types based on built-in types like 'int', 'str', and 'list' can be made using the same syntax
        that would be used to subclass other types

    It is a Hassle to implement
      - to get a type the behaves like a built-in type with some exception or added functionality can be
        tedious because many under methods might need to be changed

    Consider "Composition" instead
      - in most of the situations where subclassing a built-in type seems like a potential solution, we would
        be better served by instead creating a type the receives the built-in type as an argument and wraps it


---------------------------------------------------
3.7 Building Class Hierarchies with Inheritance in Python - Lab

Learning objectives
  - Implement the Base Question Class
  - Create the TrueFalseQuestion Class by Subclassing Question
  - Create the MultipleSelectQuestion Class by Subclassing Question

Your company wants to add a quiz system to help assess whether employees are learning anything from internal webinars. The product owner has requested the ability to create 3 types of questions:

    Multiple Choice, Single Answer - Presents x number of options with only one correct answer.
    Multiple Choice, Multiple Answer - Presents x number of options with n correct answers.
    True/False - Presents the options of True and False for the question.

A question has a few attributes:

    question_text - The text of the question.
    choices - The options that should be displayed to the user as a list.
    corect_choice - The option that is the correct answer.
    selected_answer - The currently selected option as a string

Additionally, every question object in the system needs to be able to do the following:

    Present itself to the user - Questions must be printable. The options should be randomized before they are presented to the user. Each of the three question types
      will have slight variations on how they are printed.
    Select an answer using the select method - Stores the selected option as selected_answer.
    Grade itself using the grade method - Returns True/False based on whether the selected answer and the correct choice are equal.

A co-worker has written tests that demonstrate how they would like the code to be used and to help drive the implementation. By running the test within
  test_questions.py, you'll be able to guide the implementation of the classes and know when your classes meet the requirements.

You can run the unit tests with the following command:

$ python -m unittest

.........

++++++++++++++++++++++
 code in labs/3_7_question_lab/questions.py
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
from random import sample

class Question:
    def __init__(self, question_text, choices, correct_answer) -> None:
        self.question_text = question_text
        #self.choices = [str(element) for element in choices]
        self.choices = choices
        self.correct_answer = str(correct_answer)
        self.selected_answer = None

    def __str__(self) -> str:
        output = self.question_text + "\n\n"
        choices = self.choices
        # index through randomize choices list
        for index, choice in enumerate(choices, start=0 ):
            # index + 1 because index starts at zero
            output += f"{index + 1}. {choice}\n"
        return output

    def select(self, selected_answer) -> None:
        self.selected_answer = str(selected_answer)

    def grade(self) -> bool:
        return self.correct_answer == self.selected_answer

class TrueFalseQuestion(Question):
    def __init__(self, question_text, correct_answer) -> None:
        question_text = self.__prefix_if_necessary(question_text)
        super().__init__(question_text=question_text, choices = [True, False], correct_answer = correct_answer)
        #self.choices.sort()

    def __prefix_if_necessary(self, question_text) -> str:
        if question_text.lower().startswith("false") or question_text.lower().startswith("true"):
            return question_text
        else:
            return f"True/False: {question_text}"


class MultipleSelectQuestion(Question):
    def __init__(self, question_text, choices, correct_answers) -> None:
        question_text = self.__add_text(question_text, correct_answers)
        super().__init__(question_text=question_text, choices=choices, correct_answer=correct_answers.sort())
        self.correct_answer = self.__sort_string_list(correct_answers)

    def __add_text(self, question_text, correct_answer):
        return f"{question_text} (select {len(correct_answer)})"

    def __sort_string_list (self, list_of_items):
         return list(sorted(map(str, list_of_items)))

    def select(self, selected_answer) -> None:
        self.selected_answer = self.__sort_string_list(selected_answer)

if __name__ == "__main__":
    question = Question("What's the answer to life, the universe, and everything?", [42, "Silver", "Wood", True], 42)
    question.select(42)
    print (f"question.question_text:     {question.question_text}")
    print (f"question.choices:           {question.choices}")
    print (f"question.selected_answer:   {question.selected_answer}")
    print (f"question.correct_answer:    {question.correct_answer}")
    print (f"question:                   {question}\n")
    print ()

    questiontf = TrueFalseQuestion("Ice cream is the best dessert.", True)
    questiontf.select(['list', 'random.sample'])
    print (f"questiontf.question_text:   {questiontf.question_text}")
    print (f"questiontf.choices:         {questiontf.choices}")
    print (f"questiontf.selected_answer: {questiontf.selected_answer}")
    print (f"questiontf.correct_answer:  {questiontf.correct_answer}")
    print (f"questiontf:                 {questiontf}\n")

    questionm = MultipleSelectQuestion("Which of the following functions return a list?", ['str', 'print', 'random.sample', 'list'], ['list', 'random.sample'])
    questionm.select(['list', 'random.sample'])
    print (f"questionm.question_text:   {questionm.question_text}")
    print (f"questionm.choices:         {questionm.choices}")
    print (f"questionm.selected_answer: {questionm.selected_answer}")
    print (f"questionm.correct_answer:  {questionm.correct_answer}")
    print (f"questionm:                 {questionm}\n")

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

To run 'questions.py' as main:
  $ cd labs/3_7_question_lab
  $ python questions.py

    question.question_text:     What's the answer to life, the universe, and everyth
    ing?
    question.choices:           [42, 'Silver', 'Wood', True]
    question.selected_answer:   42
    question.correct_answer:    42
    question:                   What's the answer to life, the universe, and everyth
    ing?

    1. 42
    2. Silver
    3. Wood
    4. True
    .  .   .
    .  .   .

++++++++++++++++++++++
 code in labs/3_7_question_lab/test_questions.py
    Note: this code was provided
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
from random import seed

from questions import Question, TrueFalseQuestion, MultipleSelectQuestion

import unittest

class TestQuestion(unittest.TestCase):
    def setUp(self):
        seed(1)
        self.question = Question("What's the answer to life, the universe, and everything?", [42, "Silver", "Wood", True], 42)

    def testStringOutput(self):
        self.assertEqual(str(self.question), """What's the answer to life, the universe, and everything?

1. 42
2. Silver
3. Wood
4. True
""")

    def testSelectionAndGrading(self):
        self.question.select("Silver")
        self.assertEqual(self.question.selected_answer, "Silver")
        self.assertFalse(self.question.grade())

        self.question.select(42)
        self.assertEqual(self.question.selected_answer, "42")
        self.assertTrue(self.question.grade())

class TestTrueFalseQuestion(unittest.TestCase):
    def setUp(self):
        seed(10000) # This specific seed will shuffle with False being the first option
        self.question = TrueFalseQuestion("Ice cream is the best dessert.", True)

    def testQuestionTextPrefixes(self):
        # If either True or False starts the question text then leave as is.
        # Otherwise prefix the question text with 'True/False:'
        self.assertEqual(self.question.question_text, "True/False: Ice cream is the best dessert.")
        question2 = TrueFalseQuestion("False or True: Up is down.", False)
        self.assertEqual(question2.question_text, "False or True: Up is down.")
        question3 = TrueFalseQuestion("True or False: Down is down.", True)
        self.assertEqual(question3.question_text, "True or False: Down is down.")

    def testOptionsAreTrueAndFalse(self):
        self.assertEqual(self.question.choices, [True, False])

    def testSelectionAndGrading(self):
        self.question.select(False)
        self.assertEqual(self.question.selected_answer, "False")
        self.assertFalse(self.question.grade())

        self.question.select(True)
        self.assertEqual(self.question.selected_answer, "True")
        self.assertTrue(self.question.grade())

    def testStringOutput(self):
        self.assertEqual(str(self.question), """True/False: Ice cream is the best dessert.

1. True
2. False
""")

class TestMultipleSelectQuestion(unittest.TestCase):
    def setUp(self):
        seed(1)
        self.question = MultipleSelectQuestion("Which of the following functions return a list?", ['str', 'print', 'random.sample', 'list'], ['list', 'random.sample'])

    def testSelectionAndGrading(self):
        self.question.select(['str', 'print'])
        self.assertEqual(self.question.selected_answer, ['print', 'str'])
        self.assertFalse(self.question.grade())

        # Ordering of selections shouldn't matter
        self.question.select(['random.sample', 'list'])
        self.assertEqual(self.question.selected_answer, ['list', 'random.sample'])
        self.assertTrue(self.question.grade())

        self.question.select(['list', 'random.sample'])
        self.assertEqual(self.question.selected_answer, ['list', 'random.sample'])
        self.assertTrue(self.question.grade())

    def testStringOutput(self):
        self.assertEqual(str(self.question), """Which of the following functions return a list? (select 2)

1. str
2. print
3. random.sample
4. list
""")

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

----------------------------------------------------------------------

To run 'unittest'in 'test_questions.py' on 'questions.py':
  $ cd labs/3_7_question_lab
  $ python -m unittest test_questions

    Ran 8 tests in 0.002s

    OK



# add a few errors to "test_questions.py" so it asserts:
To run 'unittest'in 'test_questions.py' on 'questions.py':
  $ python -m unittest test_questions_fail

    ======================================================================
    FAIL: testStringOutput (test_questions_fail.TestQuestion)
    ----------------------------------------------------------------------
    Traceback (most recent call last):
      File "C:\Users\pat\Documents\Online_Training\PythonTraining\pythonAdvancedObjectOrientedProgramming\labs\3_7_question_lab\test_que
    stions_fail.py", line 13, in testStringOutput
        self.assertEqual(str(self.question), """What's the answer to death, the universe, and everything?
    AssertionError: "What's the answer to life, the universe, and everything?\n\n1.[30 chars]ue\n" != "What's the answer to death, the u
    niverse, and everything?\n\n1[31 chars]ue\n"
    - What's the answer to life, the universe, and everything?
    ?                      ^^^
    + What's the answer to death, the universe, and everything?
    ?                      ^ +++

      1. 42
      2. Silver
      3. Wood
      4. True

    . . .


---------------------------------------------------

Chapter 4 Composition and Polymorphisim

---------------------------------------------------
4.1 Composition Overview


  Composition Overview
    Key #1
     - Composition is about separating concerns and responsibilities across types and combining types
    Key #2
      - Injecting object instances or classes can create flexible and resuable code
    Key #1
      - Remember the "Law of Demeter" (LoD) when utilizing composition
        - LoD recommends that objects should avoid accessing the internal data and methods of other objects.
        - Instead, an object should only interact with its immediate dependencies.

        - Know your boundaries!
          - we can call the methods that we define on our class.
          - We can call the public methods that are defined on the objects that are passed into us,
            but we can't try to call the internal methods on the things that are passed into the objects
            that are passed into us,

   Example of too Much Responsibility in One Class:

       from math import pi
       class Car:
           def __init__(self, tire_radius):
               self.tire_radius = tire_radius

           def drive(self):
               # normal car responsibility here
               pass

           def wheel_circumference(self):
               # probably not the car's job to know how calculate this
               return 2 * pi * self.tire_radius

   Using Composition to Separate Concerns

       class Car:
           def __init__(self, tire):
               self.tire = tire

           def drive(self):
               # normal car responsibility here
               pass

           def wheel_circumference(self):
               # Delegating to the 'tire' object
               return self.tire.circumference()

       from math import pi
       class Tire:
           def __init__(self, radius):
               self.radius = radius

           def wheel_circumference(self):
               return 2 * pi * self.radius

   Composition Summary
     - draw good boundaries between your objects

---------------------------------------------------
4.2 Polymorphism and Duck Typing

  Duck Typing:
    - This term comes from the saying "If it walks like a duck, and it quacks like a duck, then it must be a duck."
    - Duck typing is a concept related to dynamic typing, where the type or the class of an object is less
      important than the methods it defines.
    - When you use duck typing, you do not check types at all. Instead, you check for the presence of a given
      method or attribute.

       class Car:
           def __init__(self, tire):
               self.tire = tire

           def drive(self):
               # normal car responsibility here
               pass

           def wheel_circumference(self):
               # Delegating to the 'tire' object
               return self.tire.circumference()

       from math import pi
       class Tire:
           def __init__(self, radius):
               self.radius = radius

           def wheel_circumference(self):
               return 2 * pi * self.radius


   For Ducking typing purpose,  the above "Tire" class and the below "SnowTire" are interchangeable because
     the both implement the "circumference()" method


       from math import pi
       class SnowTire:
           def __init__(self, radius):
               self.radius = radius
               self.chain_size = chain_size

           def wheel_circumference(self):
               return 2 * pi * (self.radius + self.chain_size)


---------------------------------------------------
4.3 Composition vs Inheritance


  When to use Composition vs when to use Inheritance:
     Compositon
       - Used to create layers of abstraction and separate responsibilities
       - default to composition over inheritance
     Inheritance
       - Used to create more specific versions of a broader type.
       - allows for adding and modifying functionality of existing types by create a new type


---------------------------------------------------
4.4 Using Composition in Python to Separate Concerns - Lab

Learning objectives
  - Implement the Quiz Class with Support for Pre-Built Question Types
  - Implement the EssayQuestion Class

Your company is building a quiz system to help assess whether employees are learning anything from internal webinars. Some
classes have already been created to model various types of multiple-choice questions, and now your team is ready to start
working on the grading of an entire quiz. They've asked you to implement the Quiz class. To make this class as flexible as
possible, they've asked you to utilize composition so that Quiz itself
doesn't need to be involved in scoring individual questions.

The class must have the following attributes:

    questions - A list of questions to display to the user.
    passing_percent - The percentage of questions that must be correct to pass the quiz.
    actual_score - The number of questions that were answered correctly.

The Quiz class is responsible for doing the following:

  1 Displaying questions (randomly), one at a time to the user by using the start method. While a question is presented,
   the application should wait for user input and set the resulting message as the answer for the question before moving to
   the next question.
  2 Calculating the number of total correct answers using the score method. This should set the actual_score attribute and
    return the value.
  3 Returning whether or not the quiz was passed using the grade method. The return value should be a tuple with the type
    of (float, bool), the actual percentage of questions that were answered correctly, and the pass/fail status.

You've also been tasked with implementing an EssayQuestion class that represents a question that requires a long-form answer.
When the grade method is called, the question should print the question_text to the screen and prompt for a user to manually
grade it (giving a y or n as to whether the answer is sufficient).

To help guide your creation of this application, a coworker has written some tests to demonstrate how the classes will ideally
be used, and a sample script that can be run to see a quiz in action.

You can run all of the unit tests with the following command:

$ python -m unittest

............

----------------------------------------------------------------------

Ran 12 tests in 0.004s

OK

You can run just the quiz unit tests by running this command:

$ python -m unittest test_quiz.TestQuiz


....

Note: Did not download "test_quiz.py", so above and not be reproduced
----------------------------------------------------------------------

Ran 4 tests in 0.000s

OK

Once the Quiz and EssayQuestion classes have been implemented, you can run the example_quiz.py file to manually test how
the quiz system works.

++++++++++++++
code in: labs/4_4_quiz_lab/quiz.py
++++++++++++++++++++++++++++++++++++++++++ +
from random import sample

class Quiz:
    def __init__(self, questions, passing_percent):
        self.questions = questions
        self.passing_percent = passing_percent
        self.actual_score = 0

    def start(self):
        for index, question in enumerate(list(sample(self.questions, len(self.questions)))):
            print (f"{index + 1}. {question}")
            answer = input("Answer: ")
            # with open ("answer.txt", "a") as f:
            #    f.write (answer + "\n")
            question.select(answer)
            print("\n\n")

    def score(self):
        self.actual_score = 0
        for question in self.questions:
            if question.grade():
                self.actual_score += 1

        return self.actual_score

    def grade (self):
        self.score()
        percent = round(self.actual_score / len(self.questions), 2)
        return (percent, percent >= self.passing_percent)


from random import sample

class Question:
    def __init__(self, question_text, choices, correct_answer) -> None:
        self.question_text = question_text
        #self.choices = [str(element) for element in choices]
        self.choices = choices
        self.correct_answer = str(correct_answer)
        self.selected_answer = None

    def __str__(self) -> str:
        output = self.question_text + "\n\n"
        choices = self.choices
        # index through randomize choices list
        for index, choice in enumerate(choices, start=0 ):
            # index + 1 because index starts at zero
            output += f"{index + 1}. {choice}\n"
        return output

    def select(self, selected_answer) -> None:
        self.selected_answer = str(selected_answer)

    def grade(self) -> bool:
        return self.correct_answer == self.selected_answer

class TrueFalseQuestion(Question):
    def __init__(self, question_text, correct_answer) -> None:
        question_text = self.__prefix_if_necessary(question_text)
        super().__init__(question_text=question_text, choices = [True, False], correct_answer = correct_answer)
        #self.choices.sort()

    def __prefix_if_necessary(self, question_text) -> str:
        if question_text.lower().startswith("false") or question_text.lower().startswith("true"):
            return question_text
        else:
            return f"True/False: {question_text}"


class MultipleSelectQuestion(Question):
    def __init__(self, question_text, choices, correct_answers) -> None:
        question_text = self.__add_text(question_text, correct_answers)
        super().__init__(question_text=question_text, choices=choices, correct_answer=correct_answers.sort())
        self.correct_answer = self.__sort_string_list(correct_answers)

    def __add_text(self, question_text, correct_answer):
        return f"{question_text} (select {len(correct_answer)})"

    def __sort_string_list (self, list_of_items):
         return list(sorted(map(str, list_of_items)))

    def select(self, selected_answer) -> None:
        self.selected_answer = self.__sort_string_list(selected_answer)

if __name__ == "__main__":
    #from questions import Question, TrueFalseQuestion
    question1 = Question("What's the answer to life, the universe, and everything?", [42, "Silver", "Wood", True], 42)
    question2 = TrueFalseQuestion("Ice cream is the best dessert.", True)
    quiz = Quiz(questions=[question1, question2], passing_percent=0.50)

    from essay_question import EssayQuestion
    essay = EssayQuestion("How would you go about building a web application?")
    quiz.questions.append(essay)
    print("\nStarting quiz: answers: 42, True, 'My answer is awesome', Y\n")
    quiz.start()
    print(f"Checking quiz score: {quiz.score()}")
    print(f"Checking quiz grade: {quiz.grade()}")

++++++++++++++++++++++++++++++++++++++++++ +

++++++++++++++
code in: labs/4_4_quiz_lab/essay_question.py
++++++++++++++++++++++++++++++++++++++++++ +
class EssayQuestion:
    def __init__(self, question_text) -> None:
        self.question_text = question_text
        self.answer = None
        self.answer_is_sufficient = False

    def __str__(self) -> str:
        return self.question_text

    def select (self, answer) -> None:
        self.answer = str(answer)

    def grade(self) -> bool:
        print(f"{self.question_text}\n\nAnswer: {self.answer}\n")
        sufficient = input("Is this answer sufficent [Yn]: ")
        sufficient = True if sufficient.lower().startswith("y") else False
        print (f"sufficient:      {sufficient}")
        self.answer_is_sufficient = sufficient
        return self.answer_is_sufficient

++++++++++++++++++++++++++++++++++++++++++ +

++++++++++++++
code in: labs/4_4_quiz_lab/example_quiz.py
   Note: Provided code to run quiz
++++++++++++++++++++++++++++++++++++++++++ e
from quiz import Quiz
from questions import Question, TrueFalseQuestion
from essay_question import EssayQuestion

question1 = Question("What's the answer to life, the universe, and everything?", [42, "Silver", "Wood", True], 42)
question2 = TrueFalseQuestion("Ice cream is the best dessert.", True)
question3 = EssayQuestion("How would you scale a web application?")
quiz = Quiz(questions=[question1, question2, question3], passing_percent=0.60)

if __name__ == "__main__":
    quiz.start()
    print("Grading\n")
    percent, passing = quiz.grade()
    print(f"Results:\n\nPass: {passing} (Score: {percent * 100}%)")

++++++++++++++++++++++++++++++++++++++++++ +

To run example_quiz.py:

  $ cd labs/4_4_quiz_lab
  $ python example_quiz.py
    1. How would you scale a web application?
    Answer: auto scale

    2. True/False: Ice cream is the best dessert.

    1. False
    2. True

    Answer: True

    3. What's the answer to life, the universe, and everything?

    1. 42
    2. True
    3. Silver
    4. Wood

    Answer: 42

    Grading

    How would you scale a web application?

    Answer: auto scale

    Is this answer sufficent [Yn]: y
    sufficient:      True
    Results:

    Pass: True (Score: 100.0%)



---------------------------------------------------

Chapter 5 Attributes Encapsulation

---------------------------------------------------
5.1 Hidding Attributes

   Important Note 01
   - Sometimes, instance attributes aren't supposed to be part of the objects public API

   Important Note 02
     - single underscore attribute names are typically used to "hide" an attribue

   Important Note 03
   - Single (and double) underscore attribute names are still accessible, but convention says to not access them
   - double underscore cannot be directly access so they are somewhat 'private', but they can be accessed
     indirectly via <object>._<className><dunderVariableName>


    class A:
        def __init__(self, x):
            self._x = x
            self.__x = x * 2

    >>> a = A(2)
    # can access '_x' via 'a'
    >>> print(a._x)
    2

    # cannot access '__x' via 'a'
    >>> print(a.__x)
    Traceback (most recent call last):
      File "<stdin>", line 1, in <module>
    AttributeError: 'A' object has no attribute '__x'

    # can indirectly access '__x' via 'a._A'  (that is, via _<className><variableName>
    >>> print(a._A__x)
    4

---------------------------------------------------
5.2 Using @property '

  Python @property decorator
  https://www.programiz.com/python-programming/property

  @property decorator
    - allows to create custom readers and writers (or getters and setters) for attributes


   property  built-in function
     - In Python, property() is a built-in function that creates and returns a property object. The syntax of
       this function is:
           property(fget=None, fset=None, fdel=None, doc=None)`

           where:
              fget is function to get value of the attribute
              fset is function to set value of the attribute
              fdel is function to delete the attribute
              doc  is a string (like a comment)


  Example using property decorator to call property built-in function

        # Using @property decorator
        class Celsius:
            def __init__(self, temperature=0):
                self.temperature = temperature

            def to_fahrenheit(self):
                return (self.temperature * 1.8) + 32

            # call property gset function
            @property
            def temperature(self):
                print("Getting value...")
                return self._temperature

            # call property fset function
            @temperature.setter
            def temperature(self, value):
                print("Setting value...")
                if value < -273.15:
                    raise ValueError("Temperature below -273 is not possible")
                self._temperature = value


        # create an object
        human = Celsius(37)

        print(human.temperature)

        print(human.to_fahrenheit())

        coldest_thing = Celsius(-300)


   Property Example from this lesson (used restrict access to _salary , _hours_per_week, and _hourly_rate attributes
     Code in: "demo_code/5_2_property/properties.py"


++++++++
code in: demo_code/5_2_property/properities.py
++++++++++++++++++++++++++++++++++++++
class Employee:
    def __init__(self, name, salary, hours_per_week=40) -> None:
        self.name = name
        self._hours_per_week = hours_per_week
        self._salary = salary
        self.__set_hourly_rate()

    @property
    def hours_per_week(self):
        return self._hours_per_week

    @hours_per_week.setter
    def hours_per_week(self, value):
        self._hours_per_week = value
        self.__set_hourly_rate()

    @hours_per_week.deleter
    def hours_per_week(self):
        del self._hours_per_week

    @property
    def salary(self):
        return self._salary

    @salary.setter
    def salary(self, value):
        self._salary = value
        self.__set_hourly_rate()

    @salary.deleter
    def salary(self):
        del self._salary

    @property
    def hourly_rate(self):
        return self._hourly_rate


    # do not want hours_per_week, hour_rate, etc. to be publicly accessible
    def __set_hourly_rate(self):
        self._hourly_rate = round(self._salary / 52 / self._hours_per_week, 2)


if __name__ == '__main__':
    e = Employee('Kevin', 80000)
    print(f"Salary: {e.salary}")
    print(f"Hours Per Week: {e.hours_per_week}")
    print(f"Hourly Rate: {e.hourly_rate}")

    print(f"\nChanging salary\n")
    e.salary = 90000

    print(f"Salary: {e.salary}")
    print(f"Hours Per Week: {e.hours_per_week}")
    print(f"Hourly Rate: {e.hourly_rate}")

    print(f"\nChanging hours per week\n")
    e.hours_per_week = 35

    print(f"Salary: {e.salary}")
    print(f"Hours Per Week: {e.hours_per_week}")
    print(f"Hourly Rate: {e.hourly_rate}")

++++++++++++++++++++++++++++++++++++++


# run 'properties.py':
$ python -i demo_code/5_2_property/properties.py

  Salary: 80000
  Hours Per Week: 40
  Hourly Rate: 38.46

  Changing salary

  Salary: 90000
  Hours Per Week: 40
  Hourly Rate: 43.27

  Changing hours per week

  Salary: 90000
  Hours Per Week: 35
  Hourly Rate: 49.45


---------------------------------------------------

Chapter 6 Variadic Funtions

---------------------------------------------------
6.1 What are Variadic Functions


   Variadic Functions:
   - write functions that can capture any number of arguments

   Fuctions Handling any number of Positional Arguments

      def print_words (first, *remainder):
          print (first)
          for word in remainder:
              print (word)
      print_words("A", "B", "C", "D")

     Convention
       - The variable capturing the remaining positional arguments is traditionally called 'args'
      Data Type
        - the variable holding the extra positional arguments is 'tuple'
        - just need to know it is an iterator that can unpacked to something else, so it just like a list

   Fuctions Accepting unknown keyword arguments
      Use **kwargs
        - prefixing the final parameter in a method or function with two asterisks (**) designates that it
          will be used to capture all additional arguments
        - e.g.: captures all additional keyword argments into dictionary called "kwargs"
      Access as a dictionary
        - the variable containing the additional keyword arguments is a standard dictionary
      Expand using **
        - any dictionary can be expanded as keyword arguments for a function or method by prefixing
          the variable name with '**' when using it as argument


https://www.geeksforgeeks.org/args-kwargs-python/
*args and **kwargs in Python

  Special Symbols Used for passing arguments in Python:
    *args (Non-Keyword Arguments)
    **kwargs (Keyword Arguments)


  Python *args
    - The special syntax *args in function definitions is used to pass a variable number of arguments to a function.
    - It is used to pass a non-keyworded, variable-length argument list.

    - For example, we want to make a multiply function that takes any number of arguments and is able to multiply
      them all together. It can be done using *args.
    - Using * the variable that we associate with the * becomes iterable, meaning you can do things like iterate
      over it, run some higher-order functions such as map and filter, etc.

    Example 1:
      - Python program to illustrate *args for a variable number of arguments

        def myFun(*argv):
            for arg in argv:
                print(arg)

        myFun('Hello', 'Welcome', 'to', 'GeeksforGeeks')

        # Output:

          Hello
          Welcome
          to
          GeeksforGeeks

    Example 2:

      - Python program to illustrate *args with a first extra argument.

        def fun(arg1, *argv):
            print("First argument :", arg1)
            for arg in argv:
                print("Argument *argv :", arg)

        fun('Hello', 'Welcome', 'to', 'GeeksforGeeks')

        # Output:

          First argument : Hello
          Argument *argv : Welcome
          Argument *argv : to
          Argument *argv : GeeksforGeeks

  Python **kwargs
    - The special syntax **kwargs in function definitions is used to pass a variable length argument list.
    - We use the name kwargs with the double star **.

    - A keyword argument is where you provide a name to the variable as you pass it into the function.
    - It collects all the additional keyword arguments passed to the function and stores them in a dictionary.

      Example 1:

        def fun(**kwargs):
            for k, val in kwargs.items():
                print("%s == %s" % (k, val))

        # Driver code
        fun(s1='Geeks', s2='for', s3='Geeks')

        # Output:

          s1 == Geeks
          s2 == for
          s3 == Geeks

        - For s1=’Geeks’, s1 is key and ‘Geeks’ is a value.
        - In simple words, what we assign is value and to whom we assign is key.

      Example 2:

        def fun(arg1, **kwargs):
            for k, val in kwargs.items():
                print("%s == %s" % (k, val))

        # Driver code
        fun("Hi", s1='Geeks', s2='for', s3='Geeks')

        # Output

          s1 == Geeks
          s2 == for
          s3 == Geeks


  Using both *args and **kwargs
    - We can use both *args and **kwargs in the same function to accept a mix of positional and keyword arguments.

      Example:

        def fun(*args, **kwargs):
            print("Positional arguments:", args)
            print("Keyword arguments:", kwargs)

        fun(1, 2, 3, a=4, b=5)


        # Output

          Positional arguments: (1, 2, 3)
          Keyword arguments: {'a': 4, 'b': 5}

        - In this example, the fun can handle both positional and keyword arguments.
        - The args parameter collects positional arguments into a tuple, while the kwargs parameter collects
          keyword arguments into a dictionary.

        - My Note:
          - positional arguments, args, must preceed keyword arguments!
          - You can not intermix args and keyword args - must first specify all 'args', then specify all keyword args
          - Example:
            >>> fun(1, c=1, 2, 3, a=4, b=5)
                File "<stdin>", line 1
                  fun(1, c=1, 2, 3, a=4, b=5)
                              ^
                SyntaxError: positional argument follows keyword argument


---------------------------------------------------

Chapter 7 Decorators

---------------------------------------------------
7.1 Decorator Overview

  What are Decorators
    - A function returning another function, usually applied as a function usually applied as a function transformation
      using the @wrapper syntax
    - actually, a callable returning another callable since decorators can also wrap classes
    - when writing a decorator, you are defining a function inside a function, then returning the defined function

  An example decorator (bad use case, but it is a simple example that illustrates using a decorator)
    Code in: "demo_code/7_1_decorators/double.py"

    def double(func):
        def wrapper_func(*args, **kwargs):
            result = func(*args, **kwargs)
            return result * 2
        return wrapper_func

    def add(a, b):
        return a + b

    @double                 # equivalent to: xadd = double(xadd(a,b))
    def xadd(a, b):
        return a + b

    print(add(1, 2))
    print(xadd(1,2))

    # output
    3
    6

  Decorators in the Standard Library
    import requests
    class HttpClient:
        scheme = 'https'
        domain = 'www.example.com'

        @classmethod
        def full_url(cls, path):
                locale = cls.scheme + "://" + cls.domain
            return f"{locale}{path)"

        @staticmethod
        def get(url):
            return requests.get(url)

    @classmethod
      - used to create class method
      - define a method inside a class and attach the method to the class
      - implicitly pass in the class itself as the first argument (traditionally called 'cls')
      - used when you don't need instance-level data, but you need class-level data

    @staticmethod
      - used to create static methods
      - functions namespaced to the class, but not needing any class-level data nor instance-level data
      - decorated with the 'staticmethod' decorator ('@staticmethod')

    @property
      - let using turn methods into attributes


https://www.geeksforgeeks.org/decorators-in-python/
Decorators in Python
  - A decorator is essentially a function that takes another function as an argument and returns a new function
    with enhanced functionality.


  Decorator Example:

    # A simple decorator function
    def decorator(func):

        def wrapper():
            print("Before calling the function.")
            func()
            print("After calling the function.")
        return wrapper

    # Applying the decorator to a function
    @decorator
    def greet():
        print("Hello, World!")

    greet()

    # Output

      Before calling the function.
      Hello, World!
      After calling the function.

    Explanation:
      - decorator takes the greet function as an argument.
      - It returns a new function (wrapper) that first prints a message, calls greet() and then prints another message.
      - The @decorator syntax is a shorthand for greet = decorator(greet).

---------------------------------------------------
7.2 Stacking Decorators

   - Decorator
     - takes a callable and returns a callable

     Stacking Decorators
       - the decorator wrapping goes down the stack
         @<decorater1>
         @<decorater2>
         @<decorater3>
         def <func> (*args)

       - But, the decorator execution goes up the stack -> <func> -> @<decorator3> -> @<decorator2> -> @<decorator1>


  Stacking Decorator Example
    Code in: "demo_code/7_2_stacking_decorators/stacking_decorator.py":

    def add_four(func):
        def wrapper_func(*args, **kwargs):
            print ("We're adding 4")
            result = func(*args, **kwargs)
            return result + 4
        return wrapper_func

    def double(func):
        def wrapper_func(*args, **kwargs):
            print ("We're doubling")
            result = func(*args, **kwargs)
            return result * 2
        return wrapper_func

    @double
    @add_four                          # equivalent to: add = double(add_four(add()))
    def add (a, b):
        print(f"We're adding {a} and {b}")
        return a + b

    print(add(1, 3))

    print("\nUsing add_l")
    add_l = lambda a, b: a + b
    result = double(add_four(add_l))(1,3)
    print(result)

    # output
    # print messages indicate wrapping is going down the stack -> @double -> @add_four -> add()
    We're doubling
    We're adding 4
    We're adding 1 and 3
    # But return value, shows execution is going up the stack -> add()  1 + 3 = 4 -> @add_four 4 + 4 = 8 -> @dbouble 8 * 2 = 16
    16

    # using add_l with lambda provides the same result where the execution is: add_l() -> add_four() -> double()
    Using add_l
    We're doubling
    We're adding 4
    16


---------------------------------------------------
7.3 Creating Class-Base Decorators

  Decorator Class-Base
    - Even though the python documentation says decorator is:
       "A function returning another function, usually applied as a function usually applied as a function transformation
        using the @wrapper syntax"

    - A decorator can also wrap a class
      - this allows you to have state as part of the decoration
      - Note: the 'property' decorator is actually a class-based decorator that is using lower case name of the class
        ('property' instead of 'Property'


  Decorator class requirements:
    - a callable class MUST take a function as the argument for init
    - define the double underscore call method, '__call__', to make the instance itself a callable

  Advantage of Decorator Class
    - decorator class can be used to retain state information


  Example decorator class includoing retaining state
   Code in "demo_code/7_3_class_base_decorators/call_tracker.py"

    class CallTracker:
        def __init__(self, func) -> None:
            self.func = func
            self.times_called = 0
            self.call_args = []

        def track_call(self, *args, **kwargs):
            self.times_called += 1
            self.call_args.append((args, kwargs))

        def __call__(self, *args, **kwargs):
            self.track_call(*args, **kwargs)
            return self.func(*args, **kwargs)

    # calling @CallTracker decorator is effectively the same as executing "CallTracker(add)", but
    # except this instance 'add' is now callable
    @CallTracker
    def add(a, b):
        return a + b

    print(add(1,2))
    print(add(2, b=3))
    print(f"Times called:  {add.times_called}")
    print(f"Call Args:  {add.call_args}")

    # output - call_args shows 1st call had position args '1, 2' and no keyword args
    #                          and 2nd call had 1 positional arg '2', and 1 keyword arg, 'b=3'
    3
    5
    Times called:  2
    Call Args:  [((1, 2), {}), ((2,), {'b': 3})]


---------------------------------------------------
7.4 Decorator Classes

  Decorating Classes
   - since classes are being callables, that means that we can decorate them by returning a different callable.


  Example Decorator Classes
    Code in: "demo_code/7_4_decorator_class/decorating_class.py"

    def print_something(func):
        def wrapper_func(*args, **kwargs):
            print (f"We're running {func}")
            return func(*args, **kwargs)
        return wrapper_func

    @print_something
    class Example:
        def __init__(self, name):
            self.name = name

    example = Example("Kevin Bacon")
    print(example)

    # output
    We're running <class '__main__.Example'>
    <__main__.Example object at 0x00000207A7102FA0>


    -> the above is running the "Example" class and getting an instance back
    - it is running the 'Example' class itself because classes are callables, and it is just getting an instance back.
    - when you write a class-based decorators like the above, it is going to be decorating the class itself.
      Since the class callable returns an instance, if you do return func, whatever, then that's going to also be
      returning an instance.

  Use cases:
     1 For a class instance, if you wanted to add some attributes to an instance that was created by default without it
       actually being the normal part of the class itself, you could decorate it like this. Then you could take the
       result of calling the func and the wrapper func and tag some more attributes onto it, and then return the instance

  Useful Example of Decorating a Class
    Code also in: "demo_code/7_4_decorator_class/decorating_class.py"

    from dataclasses import dataclass
    from datetime import date

    @dataclass
    class Record:
        name: str
        artist_name: str
        release_date: date

    record1 = Record("The Beaatles", "Let it be", date(1970, 5, 8))
    record2 = Record("The Beaatles", "Let it be", date(1970, 5, 8))
    print("\n")
    print(record1.name)
    print(record1.artist_name)
    print(record1.release_date)
    print(record1 == record2)

    # output
      The Beaatles
      Let it be
      1970-05-08
      True

    -> the above code it's going to assume that name, artist_name, and release_date are all just going to be
       positional arguments.
       - So effectively, what dataclass is doing behind the scenes, is it's defining our __init__ method for us.
       - Additionally, it's also defining the comparison operators for us.
       - And all we had to use was some type hinting up here to specify that attributes are 'str' and 'date'

    -> Essentially, if you want a class to hold on to some attributes with only specifying the attribute names
       and data types (e.g. no __init__ required)

---------------------------------------------------
7.5 Decorators Summary

  Decorator Basics
    - A function returning another function, usually applied as a function usually applied as a function transformation
      using the @wrapper syntax
    - actually, a callable returning another callable since decorators can also wrap classes
       - this means you can decorate classs, and use a class to decorate a function,
    - when writing a decorator, you are defining a function inside a function, then returning the defined function

  Stacking Decorators
    - decorators are going to be applied from bottom up in the stack

    def add(a, b)
       return a + b

    add = first_decorator(add)
    add = second_decorator(add)

    - wrap from bottom to top

    @second_decorator
    @first_decorator
    def add(a, b)
       return a + b


  Class-Based Decorators

    class CallTracker:
        def __init__(self, func) -> None:
            self.func = func
            self.times_called = 0
            self.call_args = []

        def track_call(self, *args, **kwargs):
            self.times_called += 1
            self.call_args.append((args, kwargs))

        def __call__(self, *args, **kwargs):
            self.track_call(*args, **kwargs)
            return self.func(*args, **kwargs)

    1 __init__ Must receive a Function
      - the function being decorated is the first argument to the class initializer

    2 __call__ Method
      - the '__call__' method allows a class instance to be callable (and the wrapped function is a
        'CallTracker' instance everywhere we call it)

  Decorating Classes

    1 Not supper common
      - There aren't that many situations that call for decorating an entire class
    2 Essentially decorating the class constructor
      - to create an instance of a class we "call" the class constructor by using parentheses with the class name
      - We're effectively decorating that process
    3 Examples from Standard Library
      - the 'dataclass' decorator from the standard library is a good example of a class decorator

        from dataclasses import dataclass
        from datetime import date

        @dataclass
        class Record:
            name: str
            artist_name: str
            release_date: date

  Decorators with Arguments
    - when you want a decorator with default arguments or you want to configure some

    Example code
    Code also in: "demo_code/7_4_decorator_class/decorating_class.py"

        # has optional 'func' function input, and '*" means it does not allow positional argument
        def log_to_file(func = None, *,, file_name='default_file.txt'):
            def decorator_func(func):
                def wrapper_func(*args, **kwargs):
                    with open(file_name, "a") as f:
                        output = func.__name__ + \
                            f"called with Args: {args}" + \
                            f" called with Kwargs: {kwargs}\n"
                        f.write(output)
                    return func(*args, **kwargs)
                return wrapper_func
            if func:
                return decorator_func(func)
            else:
                return decorator_func


---------------------------------------------------
7.6 Benchmarking Functions - Lab

Learning objectives
  - Implement the log Decorator
  - Implement the benchmark Decorator

Additional Resources

As you and your team have been working on your internal applications, you've found yourself wanting to be able to quickly benchmark and
add logging to a function to help in your debugging. Your team has decided that they'd like to have a library of decorators that they can
easily pull in to add logging and benchmarking to a function temporarily without needing to actually modify the function. You've been tasked
with building these decorators.

Here are the decorators that you need to build and their requirements.

    benchmark - Will note the start time and completion time of the wrapped function and print (or write to a file) the following line
                when the function is called:

                benchmark: [wrapped_function_name] duration: [total_runtime_in_seconds]

    log - Simply prints (or writes to a file) the message when the function is called.

         running: [wrapped_function_name], args: [positional_arguments], kwargs: [keyword_arguments]

For both of these decorators, you should be able to provide file_name as an optional keyword argument to state the file that the benchmark
or log lines should be written.

Implement the log Decorator

   The log decorator needs to print (or write to a file) a line in the following format:

       running: [wrapped_function_name] args: [positional_arguments] kwargs: [keyword_arguments]

   The function should allow for an optional file_name argument so that it can be used like this:

      @log(file_name="log_file.txt")
      def some_functions(a, b):
          pass

Implement the benchmark Decorator

   The benchmark decorator needs to print (or write to a file) a line in the following format:

      benchmark: [wrapped_function_name] duration: [total_runtime_in_seconds]

   The function should allow for an optional file_name argument so that it can be used like this:

      @benchmark(file_name="benchmarks.txt")
      def some_functions(a, b):
          pass

+++++++++++
code in: labs/7_6_decorator_lab/decorator.py (created file + my testing at end)
++++++++++++++++++++++++++++++++++++
def log(func=None, *, file_name=None):
    def decorator_func(func):
        def wrapped_func(*args, **kwargs):

            result = func(*args, **kwargs)

            message = f"running: {func.__name__} args: {args} kwargs: {kwargs}, result: {result}"

            if file_name:
                with open(file_name, 'a') as f:
                    f.write(message + "\n")

            else:
                print(message)

            return result

        return wrapped_func
    if func:
        return decorator_func(func)
    else:
        return decorator_func

from datetime import datetime

def benchmark(func=None, *, file_name=None):
    def decorator_func(func):
        def wrapped_func(*args, **kwargs):
            start_time = datetime.now()
            result = func(*args, **kwargs)
            end_time = datetime.now()
            duration = round((end_time - start_time).total_seconds(), 1)
            message = f"benchmark: {func.__name__} duration: {duration}"

            if file_name:
                with open(file_name, 'a') as f:
                    f.write(message + "\n")

            else:
                print(message)

            return result

        return wrapped_func
    if func:
        return decorator_func(func)
    else:
        return decorator_func


if __name__ == '__main__':
    import time
    import os
    print("\nTesting log decorator with 'add' function")
    @log
    def add(a, b):
        return a + b

    add(1, b=2)

    print("\nTesting log decorator with 'add' function and output file")
    file_name="log.txt"
    @log(file_name=file_name)
    def add(a, b):
        return a + b

    add(1, b=2)
    print(f"Printing '{file_name}' contents, then deleting it")
    if os.path.exists(file_name):
        print(open(file_name).read())
        os.remove(file_name)
    else:
        print(f"Error: {file_name} does not exist")


    print("\nTesting benchmark decorator with 'add' function")
    @benchmark
    def add(a, b):
        time.sleep(2)
        return a + b

    add(1, b=2)

    print("\nTesting benchmark decorator with 'add' function and output file")
    file_name="benchmark.txt"
    @benchmark(file_name=file_name)
    def add(a, b):
        time.sleep(2)
        return a + b

    add(1, b=2)
    print(f"Printing '{file_name}' contents, then deleting it")
    if os.path.exists(file_name):
        with open(file_name, 'r') as f:
            print(f.read())
        os.remove(file_name)
    else:
        print(f"Error: {file_name} does not exist")

++++++++++++++++++++++++++++++++++++


# run 'decorators.py':
$ python labs/7_6_decorator_lab/decorators.py

  Testing log decorator with 'add' function
  running: add args: (1,) kwargs: {'b': 2}, result: 3

  Testing log decorator with 'add' function and output file
  Printing 'log.txt' contents, then deleting it
  running: add args: (1,) kwargs: {'b': 2}, result: 3


  Testing benchmark decorator with 'add' function
  benchmark: add duration: 2.0

  Testing benchmark decorator with 'add' function and output file
  Printing 'benchmark.txt' contents, then deleting it
  benchmark: add duration: 2.0



+++++++++++
code in: labs/7_6_decorator_lab/test_decorators.py  (provided code for testing)
++++++++++++++++++++++++++++++++++++
import unittest
import io
import time

from tempfile import TemporaryDirectory
from unittest.mock import patch

from decorators import benchmark, log

class TestDecorators(unittest.TestCase):
    def setUp(self):
        self.tempdir = TemporaryDirectory()

    def tearDown(self) -> None:
        self.tempdir.cleanup()
        return super().tearDown()

    @patch('sys.stdout', new_callable=io.StringIO)
    def testLog(self, mock_stdout):
        @log
        def add(a, b):
            return a + b

        add(1, b=2)

        mock_stdout.seek(0)
        written = mock_stdout.read()
        self.assertEqual(written, "running: add args: (1,) kwargs: {'b': 2}, result: 3\n")


    def testLogWithFile(self):
        file_name = self.tempdir.name + "/log.txt"
        @log(file_name=file_name)
        def add(a, b):
            return a + b

        add(1, b=2)

        with open(file_name) as f:
            written = f.read()
            self.assertEqual(written, "running: add args: (1,) kwargs: {'b': 2}, result: 3\n")


    @patch('sys.stdout', new_callable=io.StringIO)
    def testBenchmark(self, mock_stdout):
        @benchmark()
        def add(a, b):
            time.sleep(2)
            return a + b

        add(1, b=2)

        mock_stdout.seek(0)
        written = mock_stdout.read()
        self.assertEqual(written, "benchmark: add duration: 2.0\n")


    def testBenchmarkWithFile(self):
        file_name = self.tempdir.name + "/benchmark.txt"

        @benchmark(file_name=file_name)
        def add(a, b):
            time.sleep(2)
            return a + b

        add(1, b=2)

        with open(file_name) as f:
            written = f.read()
            self.assertEqual(written, "benchmark: add duration: 2.0\n")


++++++++++++++++++++++++++++++++++++

# TestDecorators unittest may fail because run time for 'add duration' is hardcoded to 2.0 [seconds]
# run TestDecorators unittest:
$ cd labs/7_6_decorator_lab
$ python -m unittest test_decorators.TestDecorators
  ....
  ----------------------------------------------------------------------
  Ran 4 tests in 4.023s

  OK

---------------------------------------------------

Chapter 8 Comparing and Copying Objects

---------------------------------------------------
8.1 Comparison and Variable Referencing

  No Pass[ing] by Reference [in Python]
    - in some programming languages [e.g. C++], you have access to pointers where you can pass the pointer to a
      variable, and not the value of a variable, into something else
    - In Python, you can see where something is pointed to in memory by looking at its ID, that's its identifier.

    python id() method
      - The id() method returns a unique integer (identity) [memory location] of a passed argument object.

      - syntax: id(object)
         where: object - can be a class, variable, list, tuple, set, etc.

      - id() Return Value
        - the identity of the object (which is a unique integer for a given object)

    Example of using python 'id'

        def increment(a):
            print(f"Id of a is {id(a)}")
            a += 1
            print(f"Id of a is {id(a)}")
        x = 1
        print(f"Id of x is {id(x)}")
        increment(x)
        print(f"Id of x is {id(x)}")

        # print initial id of 'x'
        >>> x = 1
        >>> print(f"Id of x is {id(x)}")
        Id of x is 2097007782192


        # print id of 'a' before it increments (id same as 'x') and after (id changed)
        >>> increment(x)
        Id of a is 2097007782192
        Id of a is 2097007782224

        # print id of 'x' after it was updated by increment() and it has original id
        >>> print(f"Id of x is {id(x)}")
        Id of x is 2097007782192
        >>>

  Variable Referencing
    Mutable
      - if passed to a function, the argument can be modified in-place
      - if you pass a mutable variable to a function, it can modify a mutable variable
    Immutable
      - cannot be modified by a function when passed in as an argument, ever


  Note: integer variable id changes after addition or other change to variable's value because integers are
        immutable objects

        >>> y = 0
        >>> print(f"Id of x is {id(y)}")
            Id of x is 2709233363216
        >>> y =+ 1
        >>> y
            1
        >>> print(f"Id of x is {id(y)}")
            Id of x is 2709233363248

   AI Overview: (to: python why does variable id change after addition to variable)

     - In Python, the id of a variable can change after an addition operation because integers are immutable. 
     - When you perform an addition operation (e.g., x = x + 1), you are not modifying the original integer object; 
       instead, you are creating a new integer object with the updated value and assigning it to the variable. 
     - Since it's a new object, it has a different id.

        Python
        >>> x = 5
        >>> print(id(x))
            2709233363376
        >>> x = x + 1
        >>> print(id(x))
            2709233363408

      - This behavior is specific to immutable data types like integers, floats, strings, and tuples. 
      - Mutable data types like lists and dictionaries behave differently. When you modify a mutable object, 
        its id remains the same because you are changing the object in place, rather than creating a new one.

        Python
        >>> list_a = [1, 2, 3]
        >>> print(id(list_a))
            2709234451136
        >>> list_a.append(4)
        >>> print(id(list_a))
            2709234451136

---------------------------------------------------
8.2 ==, is, and id

  Python 'integers' are immutable data types
    - as a result, python id() will point at the same location for variable set to the same integer value

    >>> a = 1
    >>> b = 1
    >>> a == b
    True

    # id(a) is the same as the id(b) because both set to the integer '1'
    >>> id(a)
        2097007782192
    >>> id(b)
        2097007782192
    # since 'a' and 'b' id() are the same, 'a is b' result is 'True'
    >>> a is b
        True

  Python 'dict' are mutable data types
    - as a result, python id() will point at different locations for dict's set to the same value

    >>> a = {'a': 1}
    >>> b = {'a': 1}
    >>> a == b
        True

    # id(a) is different from the id(b) even though both are set to the same value, but they
    #   are dict's, mutable data type
    >>> id(a)
        2097013752576
    >>> id(b)
        2097013752704

    # since 'a' and 'b' id() are different, 'a is b' result is 'False'
    >>> a is b
        False

  Python 'is' method:
      - '<object1> is <object2>' will only return true if the id(<object1>) matches the id(<object2>)

  Python '==' method:
      - '<object1> == <object2>' will return true if the <object1>'s value matches <object2>'s value

---------------------------------------------------
8.3 Slicing, Copying, and Deep Copying

   Code is in 'demo_code/8_3_slicing_copying_deep_copying/copying.py'


   Passing a 'dict' to function withOUT copy
     - when a mutable type, ['dict'] is passed to a function, the function returns a modified 'dict',
       but the input 'dict' is also modified since the input mutable type was not copied first

       four_sided = {
           "name": "A 4 sided die",
           "options": [1, 2, 3, 4]
       }

       def changeDie(die):
           die["name"] = "A 6 sided die"
           die["options"].append(5)
           die["options"].append(6)
           return die

       print("\nUsing changeDie without copy\n")
       new_die = changeDie(four_sided)
       print(four_sided)
       print(new_die)

       # output NOT using copy.copy()
       #   'four_sided' (input to 'changeDie' function) is modified
       #   'new_die' (output to 'changeDie' function) is modified (expected)
       Using changeDie without copy
       {'name': 'A 6 sided die', 'options': [1, 2, 3, 4, 5, 6]}
       {'name': 'A 6 sided die', 'options': [1, 2, 3, 4, 5, 6]}

   Passing a 'dict' to function WITH copy
     - when a mutable type, ['dict'] is passed to a function, the function first copies the mutable input
       and then modiifies it and then returns a modified 'dict',  the input 'dict' is not modified EXCEPT for
       mutable type inside the 'dict'

       import copy
       four_sided = {
           "name": "A 4 sided die",
           "options": [1, 2, 3, 4]
       }

       def changeDie(die):
           die = copy.copy(die)
           die["name"] = "A 6 sided die"
           die["options"].append(5)
           die["options"].append(6)
           return die
       print("\nUsing changeDie with copy\n")
       new_die = changeDie(four_sided)
       print(four_sided)
       print(new_die)

       # output - using copy.copy()
       #  'four_sided' (input to 'changeDie' function) is partial modified - 'name' is not modified, 'options' is modified
       #  'new_die' (output to 'changeDie' function) is modified (expected)
       Using changeDie with copy
       {'name': 'A 4 sided die', 'options': [1, 2, 3, 4, 5, 6]}
       {'name': 'A 6 sided die', 'options': [1, 2, 3, 4, 5, 6]}

   Passing a 'dict' to function WITH deepcopy
     - when a mutable type, ['dict'] is passed to a function, the function first 'deepcopy' the mutable input
       and then modiifies it and then returns a modified 'dict',  the input 'dict' is NOT modified

       four_sided = {
           "name": "A 4 sided die",
           "options": [1, 2, 3, 4]
       }

       def changeDie(die):
           die = copy.deepcopy(die)
           die["name"] = "A 6 sided die"
           die["options"].append(5)
           die["options"].append(6)
           return die
       print("\nUsing changeDie with deepcopy\n")
       new_die = changeDie(four_sided)
       print(four_sided)
       print(new_die)

       # output - using copy.deepcopy
       #  'four_sided' (input to 'changeDie' function) is NOT modified
       #  'new_die' (output to 'changeDie' function) is modified (expected)
       Using changeDie with deepcopy
       {'name': 'A 4 sided die', 'options': [1, 2, 3, 4]}
       {'name': 'A 6 sided die', 'options': [1, 2, 3, 4, 5, 6]}



   Slicing a list create a unique new list

       print("\nUsing slicing to create a unique new list")
       my_list = [1, 2, 3, 4]
       my_other_list = my_list[:]
       print(f"my_other_list == my_list\n{my_other_list == my_list}")
       print(f"my_other_list is my_list\n{my_other_list is my_list}")

       # output
       Using slicing to create a unique new list
       my_other_list == my_list
       True
       my_other_list is my_list
       False

++++++++++++++++++++++++++++++++++++

# run 'copying.py':
$ python demo_code/8_3_slicing_copying_deep_copying/copying.py
  Using changeDie without copy
  
  {'name': 'A 6 sided die', 'options': [1, 2, 3, 4, 5, 6]}
  {'name': 'A 6 sided die', 'options': [1, 2, 3, 4, 5, 6]}
  
  Using changeDie with copy
  
  {'name': 'A 4 sided die', 'options': [1, 2, 3, 4, 5, 6]}
  {'name': 'A 6 sided die', 'options': [1, 2, 3, 4, 5, 6]}
  
  Using changeDie with deepcopy
  
  {'name': 'A 4 sided die', 'options': [1, 2, 3, 4]}
  {'name': 'A 6 sided die', 'options': [1, 2, 3, 4, 5, 6]}
  
  Using slicing to create a unique new list
  my_other_list == my_list
  True
  my_other_list is my_list
  False

---------------------------------------------------
8.4 Cloning and Modifying Mutable Objects in Python - Lab

Learning objectives
  - Parse the Template JSON File
  - Prompt for the Values That Need to Be Changed
  - Export New Data to a New JSON File

Additional Resources

A friend of yours works in real estate and uses a tool that allows them to drag in a JSON file with all of the
information about a new property listing. There aren't that many fields that need to be changed between each one,
but they hate modifying a JSON file because they feel like they're going to mess something up and have no idea how
to fix it. Looking at their situation, you've convinced them that the command line isn't as scary as they think and
told them that you can write a tool that will prompt them for the information that they always change and generate
a new JSON document as long as they provide an example for you.

When creating a new listing, the following pieces of data always need to be changed:

    listing_date    - This should always be tomorrow when the application is run.
    property        - This is a complex data structure with quite a few options that will all need to be modified.
    broker.referrer - There may or may not be a referrer for this listing.

You'd like to eventually take this logic and put it behind a long-running web application so you know that it is
important that the variable holding onto the template data doesn't change at any point during the process. If that
happened for a web application, then each request might change the outcome for the next request unintentionally.

There are some automated tests for this process. You can run all of the unit tests with the following command:

$ python -m unittest  test_real_estate.TestMain

..

Parse the Template JSON File

There should always be a template.json file next to the script so that it can get an up-to-date picture of the
values that don't change. When the script is initially run, this should be parsed and stored as the template variable.

Prompt for the Values That Need to Be Changed

When the tool is run, it should present the user with a series of prompts like this:

$ python real_estate.py
Address
Street 1: [prompt]
Street 2: [prompt]
City: [prompt]
State: [prompt]
ZIP: [prompt]

Details
Square Footage: [prompt]
Bedrooms: [prompt]
Bathrooms: [prompt]
Ammenities (use | between items): [prompt]

Was there a referrer? (Y or n): [prompt]
Referrer: [prompt ONLY if Y to previous]

Export New Data to a New JSON File

Write the dictionary with the new values to a JSON file named after the street address of the property with the spaces converted to dashes.


----------------------------------------------------------------------

Ran 2 tests in 0.004s



OK


++++++++
code in: labs/8_4_mutable_objects_lab/real_estate.py (created)
+++++++++++++++++++++++++++++++++++++++++++

from json import load, dump, dumps
import copy
from datetime import date, timedelta

json_file_name = "template.json"

# This variable must be defined and populated for tests.
template = None
with open(json_file_name) as json_file:
    template = load(json_file)

def gather_property_info():
    print("Address")
    street_1 = input("Street 1: ")
    street_2 = input("Street 2: ")
    city = input("City: ")
    state = input("State: ")
    zip_code = input("ZIP: ")

    print("\nDetails")
    square_footage = int(input("Square Footage: ").replace(",",""))
    bedrooms = int(input("Bedrooms: "))
    bathrooms = int(input("Bathrooms: "))
    ammenities = list(
        map(str.strip, input("Ammenities (use | between items): ").split("|"))
    )

    return {
        "address": {
            "street_1": street_1,
            "street_2": street_2,
            "city": city,
            "state": state,
            "zip": zip_code,
        },
        "square_footage": square_footage,
        "bedrooms": bedrooms,
        "bathrooms": bathrooms,
        "ammenities": ammenities,
    }

def gather_referrer_info():
    has_referrer = input("Was there a referrer? (Y or N): ")
    if has_referrer.lower().startswith("y"):
        return input("Referrer: ")
    else:
        return None

def main():
    # Place all logic in here (or in functions called here)
    output = copy.deepcopy(template)
    property_info = gather_property_info()
    referrer = gather_referrer_info()
    output["listing_date"] = str(date.today() + timedelta(days=1))
    output["property"] = property_info
    output["broker"]["referrer"] = referrer

    output_file_name = property_info["address"]["street_1"].replace(" ", "-") + ".json"
    output_indent_file_name = property_info["address"]["street_1"].replace(" ", "-") + "-indent.json"

    print(output)

    with open(output_file_name, "w") as output_file:
        dump(output, output_file)
        print(f"Created {output_file_name}")

    json_object = dumps(output, indent=4)

    with open(output_indent_file_name, "w") as output_file:
        output_file.write(json_object)
        print(f"Created {output_indent_file_name}")

if __name__ == "__main__":
    main()

+++++++++++++++++++++++++++++++++++++++++++

# Note: enter values to prompt shown in [enter values]
# run "real_estate.py"
$ cd  /labs/8_4_mutable_objects_lab
$ python real_estate.py
  Address
  Street 1: [100 UpACreek Rd]
  Street 2: [Unit TBD]
  City:     [Creekville]
  State:    [CA]
  ZIP:      [95001]

  Details
  Square Footage:    [3333]
  Bedrooms:          [4]
  Bathrooms:         [3]
  Ammenities (use | between items):     [Creek view | Creek room | gold toilet]
  Was there a referrer? (Y or N):       [y]
  Referrer:                             [Jon Creek II]
  {'listing_date': '2025-04-02', 'property': {'address': {'street_1': '100 UpACreek Rd', 'street_2': 'Unit TBD', 'city': 'Creekville',
   'state': 'CA', 'zip': '95001'}, 'square_footage': 3333, 'bedrooms': 4, 'bathrooms': 3, 'ammenities': ['Creek view', 'Creek room', 'gold toilet']}, 
   'broker': {'name': 'Tonya Sullivan', 'license_number': 'ABCD1234', 'address': {'street_1': '100 Fake St', 'street_2'
   : None, 'city': 'Boston', 'state': 'MA', 'zip': '90210'}, 'referrer': 'Jon Creek II'}}
  Created 100-UpACreek-Rd.json
  Created 100-UpACreek-Rd-indent.json

+++++++++++++++++++++++++++++++++++++++++++

++++++++
code in: labs/8_4_mutable_objects_lab/test_real_estate.py (provided)
+++++++++++++++++++++++++++++++++++++++++++

import unittest
import sys
import io

from copy import deepcopy
from json import load
from datetime import date, timedelta
from unittest.mock import patch

from real_estate import template, main


class TestMain(unittest.TestCase):
    def setUp(self) -> None:
        super().setUp()
        self.maxDiff = None

    @patch("sys.stdout", new_callable=io.StringIO)
    @patch(
        "builtins.input",
        side_effect=[
            "1600 Pennsylvania Ave NW",
            "",
            "Washington",
            "DC",
            "20500",
            "54,900",
            "5",
            "35",
            "3 Kitchens | 28 fireplaces | 3 elevators | 8 staircases",
            "n",
        ],
    )
    def testMainDoesntChangeTemplate(self, mock_stdout, mock_input):
        tomorrow = date.today() + timedelta(days=1)
        orig_template = deepcopy(template)

        main()

        self.assertEqual(orig_template, template)

        with open("1600-Pennsylvania-Ave-NW.json") as json_file:
            output = load(json_file)
            self.assertEqual(
                output,
                {
                    "listing_date": str(tomorrow),
                    "property": {
                        "address": {
                            "street_1": "1600 Pennsylvania Ave NW",
                            "street_2": "",
                            "city": "Washington",
                            "state": "DC",
                            "zip": "20500",
                        },
                        "square_footage": 54900,
                        "bedrooms": 5,
                        "bathrooms": 35,
                        "ammenities": [
                            "3 Kitchens",
                            "28 fireplaces",
                            "3 elevators",
                            "8 staircases",
                        ],
                    },
                    "broker": {
                        "name": "Tonya Sullivan",
                        "license_number": "ABCD1234",
                        "address": {
                            "street_1": "100 Fake St",
                            "street_2": None,
                            "city": "Boston",
                            "state": "MA",
                            "zip": "90210",
                        },
                        "referrer": None,
                    },
                },
            )

    @patch("sys.stdout", new_callable=io.StringIO)
    @patch(
        "builtins.input",
        side_effect=[
            "1600 Pennsylvania Ave NW",
            "",
            "Washington",
            "DC",
            "20500",
            "54,900",
            "5",
            "35",
            "3 Kitchens | 28 fireplaces | 3 elevators | 8 staircases",
            "Y",
            "Kevin Bacon",
        ],
    )
    def testMainHandlesReferrer(self, mock_stdout, mock_input):
        tomorrow = date.today() + timedelta(days=1)
        orig_template = deepcopy(template)

        self.assertIsNotNone(orig_template)

        main()

        self.assertEqual(orig_template, template)

        with open("1600-Pennsylvania-Ave-NW.json") as json_file:
            output = load(json_file)

            self.assertEqual(
                output,
                {
                    "listing_date": str(tomorrow),
                    "property": {
                        "address": {
                            "street_1": "1600 Pennsylvania Ave NW",
                            "street_2": "",
                            "city": "Washington",
                            "state": "DC",
                            "zip": "20500",
                        },
                        "square_footage": 54900,
                        "bedrooms": 5,
                        "bathrooms": 35,
                        "ammenities": [
                            "3 Kitchens",
                            "28 fireplaces",
                            "3 elevators",
                            "8 staircases",
                        ],
                    },
                    "broker": {
                        "name": "Tonya Sullivan",
                        "license_number": "ABCD1234",
                        "address": {
                            "street_1": "100 Fake St",
                            "street_2": None,
                            "city": "Boston",
                            "state": "MA",
                            "zip": "90210",
                        },
                        "referrer": "Kevin Bacon",
                    },
                },
            )


if __name__ == "__main__":
    unittest.main()


+++++++++++++++++++++++++++++++++++++++++++

# run provided unittest:
$ python -m unittest  test_real_estate.TestMain

..
----------------------------------------------------------------------
Ran 2 tests in 0.012s

OK

+++++++++++++++++++++++++++++++++++++++++++

---------------------------------------------------

Chapter 9 Abstract Classes and Metaprogramming

---------------------------------------------------
9.1 Overview of Abstract Classes

  abc [module] - Abstract Base Classes
    https://docs.python.org/3/library/abc.html
    - The 'abc' module provides the infrastructure for defining abstract base classes (ABCs) in Python

  Abstract Base Class Glossary entry
    https://docs.python.org/3/glossary.html#term-abstract-base-class

  What are Abstract Classes?
    - Abstract base classes introduce virtual subclasses, which are classes that don't inherit from a
      class but are still recognized by isinstance() and issubclass()
    - not a concrete class - works alot like an interface from other programming languages

  Creating and Using an Abstract Class

    Example Abstract Class
      Code in "demo_code/9_1_abstract_classes/abstract.py"

        from abc import ABC, abstractmethod, abstractclassmethod, abstractstaticmethod

        class APIClient(ABC):
            def __init__(self, domain):
                self.domain = domain

            @abstractmethod
            def get(self, url):
                pass

            @abstractclassmethod
            def default_headers(cls):
                pass

            @abstractstaticmethod
            def static_option():
                pass

        class MyClient(APIClient):
            pass


        # if you try to create an "MyClient", you get because 'MyClient' must first implement:
           # 'get' method, 'default_headers' classmethod, and 'static_option' staticmethod
        $ python -i demo_code/9_1_abstract_classes/abstract.py
        >>> client = MyClient("https://google.com")
            Traceback (most recent call last):
              File "<stdin>", line 1, in <module>
            TypeError: Can't instantiate abstract class MyClient with abstract methods default_headers, get, static_option


     - abstract base class is used to enforce polymorphism in the objects that you want to accept.
     - that is, it enforce the inheriting class to implement specified methods (via @abstractmethod),
       classmethods (via @abstractclassmethods, and staticmethods (via @abstractstaticmethods)

---------------------------------------------------
9.2 What is Metaprogramming

  What is Metaprogramming?
    - metaprogramming is a programming technique in which programs have the ability to essentially modify themselves.
    - This can mean that it can be used to generate code, analyze and transform code, or even change its own
      logic as it's running.
    - complicated because it involves code that can create or change code

  Metaprogramming in Python

    All about Metaclasses
      - what is the type of class itself?

       The 'type' class
         - every class in python is an instance of the 'type' class

       Metaclasses
         - 'type' defines how a class is created and is the default metaclass for all classes in python

       Custom Metaclasses
         - we can customize how a class is made by defining and using a new metaclass

  Creating a Class

      class Foo:
          def __init__(self, name):
              self.name = name

          def print_name(self):
              print(self.name)

  Creating a Class Dynamically
    - 'type' constructer can handle 3 args: A name, base classes, and attributes (passed in via a 'dict')

      def __init__(self, name):
          self.name = name

      def print_name(self):
          print(self.name)

      Foo = type("Foo", (), {
          "__init__" : foo_init,
          "print_name" : foo_print_name,
      })

  Why create Metaclasses?

    1 Code Generation
      - the default metaclass builds every class the same way
      - But a custom metaclass could dynamically generate functions for a class

    2 That's About it
      - you will almost never need to use metaprogramming because most of what it would be used for can be
        done with a decorator or inheritance

---------------------------------------------------
9.3 The type class

  The 'type' built-in [class type]
    https://docs.python.org/3/library/functions.html#type

  The '__new__' method
    https://docs.python.org/3/reference/datamodel.html#object.__new__
    - object.__new__(cls[, ...])
    https://www.geeksforgeeks.org/__new__-in-python/
    - Whenever a class is instantiated __new__ and __init__ methods are called.
    - __new__ method will be called when an object is created and __init__ method will be called to initialize
      the object.
    - In the base class object, the __new__ method is defined as a static method which requires to pass a
      parameter cls. cls represents the class that is needed to be instantiated, and the compiler automatically
      provides this parameter at the time of instantiation.

  The 'dataclasses' module
    https://docs.python.org/3/library/dataclasses.html
    - The dataclasses module provides a decorator and functions for automatically adding generated special methods
      such as __init__() and __repr__() to user-defined classes

  Dataquest: How to Use Python Data Classes in 2023 (A Beginner’s Guide)
  https://www.dataquest.io/blog/how-to-use-python-data-classes/
    - a data class is a class that is designed to only hold data values
    - They aren't different from regular classes, but they usually don't have any other methods. 
    - They are typically used to store information that will be passed between different parts of a program or a system.

    Example: implementing a class to store data about a certain group of people

        class Person():
        def __init__(self, name, age, height, email):
            self.name = name
            self.age = age
            self.height = height
            self.email = email

    Example: Using dataclasses model to implement a class to store data about a certain group of people
        from dataclasses import dataclass

        @dataclass
        class Person():
            name: str
            age: int
            height: float
            email: str

    Example: Using dataclasses model with default values to implement a class to store data about a certain group of people
        @dataclass
        class Person():
            name: str = 'Joe'
            age: int = 30
            height: float = 1.85
            email: str = 'joe@dataquest.io'

        print(Person())
          # output
          Person(name='Joe', age=30, height=1.85, email='joe@dataquest.io')

    Example: Instantiate a new object and access its attributes:
        person = Person('Joe', 25, 1.85, 'joe@dataquest.io')
        print(person.name)
          # output
          Joe

  pdb - The python Debugger
    https://docs.python.org/3/library/pdb.html
    - The module pdb defines an interactive source code debugger for Python programs.
    - It supports setting (conditional) breakpoints and single stepping at the source line level, inspection
      of stack frames, source code listing, and evaluation of arbitrary Python code in the context of any stack frame.
    - It also supports post-mortem debugging and can be called under program control.

So if anything has a default value, it's going to be set as a variable that has an actual value, and
we should be able to access that through the __ dict portion. So let's, let's go ahead and do a dictionary
and comprehension here for just key value, not valueError, key value for key, value in final.__dict.__items(),


  metaclass
    https://www.datacamp.com/tutorial/python-metaclasses
    - A metaclass in Python is a class of a class that defines how a class behaves.
    - A class is itself an instance of a metaclass.
    - A class in Python defines how the instance of the class will behave.

    - Python creates the classes using a metaclass.
    - everything in Python is an object, these objects are created by metaclasses.
    - Whenever class is called to create a class, there is a metaclass that does the magic of creating the
      class behind the scenes.  It is similar to str that creates strings and int that creates integers.
    - In Python, the ___class__attribute enables us to check the type of the current instance.

  type()
    https://www.geeksforgeeks.org/python-type-function/
    - type in Python enables us to find the type of an object.
    - If a single argument type(obj) is passed, it returns the type of the given object.

    - If three argument types (object, bases, dict) are passed, it returns a new type object.
    - type() with three arguments can be used to dynamically initialize classes or existing classes with attributes

    Syntax: type(object, bases, dict)
      Parameters :
        object: Required. If only one parameter is specified, the type() function returns the type of this object
        bases : tuple of classes from which the current class derives. Later corresponds to the __bases__ attribute.
        dict : a dictionary that holds the namespaces for the class. Later corresponds to the __dict__ attribute.

  dataclasses
    https://www.geeksforgeeks.org/understanding-python-dataclasses/
    - added in python 3.7 as a utility tool for storing data
    - DataClasses provides a decorator and functions for automatically adding generated special methods such
      as __init__() , __repr__() and __eq__() to user-defined classes.
    - DataClasses are like normal classes in Python, but they have some basic functions like instantiation,
      comparing, and printing the classes already implemented.

    Syntax: @dataclasses.dataclass(*, init=True, repr=True, eq=True, order=False, unsafe_hash=False, frozen=False)
      Parameters:
        init: If true  __init__() method will be generated
        repr: If true  __repr__() method will be generated
        eq: If true  __eq__() method will be generated
        order: If true  __lt__(), __le__(), __gt__(), and __ge__() methods will be generated.
        unsafe_hash: If False __hash__() method is generated according to how eq and frozen are set
        frozen: If true assigning to fields will generate an exception.

   locals()
     https://www.geeksforgeeks.org/python-locals-function/
     - returns the dictionary of the current local symbol table. Use to access the local symbol table in Python.
       Symbol table: It is a data structure created by a compiler that is used to store all information needed to
             execute a program.
       Local symbol Table: This symbol table stores all information needed for the local scope of the program
             and this information is accessed using the Python built-in function locals().




  # 
  # 'record_newdataclass.py" with pdb.set_trace() breakpoint:
  #   Note: Uncomment the line 10:      import pdb; pdb.set_trace()
  #         Return: returns a new type class or essentially a metaclass.
  #
  $  python demo_code/9_3_type_class/record_newdataclass.py
      > c:\users\pat\documents\online_training\pythontraining\pythonadvancedobjectorientedprogramming\demo_code\9_3_type_class\record_newd
ataclass.py(12)__new__()
      -> defaults = {
    (Pdb) final
      <class '__main__.Record'>
    (Pdb) dir(final)
      ['__annotations__', '__class__', '__delattr__', '__dict__', '__dir__', '__doc__', '__eq__', '__format__', '__ge__', '__getattribute_
_', '__gt__', '__hash__', '__init__', '__init_subclass__', '__le__', '__lt__', '__module__', '__ne__', '__new__', '__reduce__', '__r
educe_ex__', '__repr__', '__setattr__', '__sizeof__', '__str__', '__subclasshook__', '__weakref__', 'album_color', 'release_date']
    (Pdb) exit()


++++++++
code in: demo_code/9_3_type_class/record_newdataclass.py

   Notes: 
      - Original code was 'new_dataclass.py' and created a 'DataClass' instead of 'NewDataClass'
      - This code shows how 'dataclass' code could be implemented that performs similar functionality to the 'dataclasses' using 
        the @dataclass decorator provides
       
+++++++++++++++++++++++++++++++++++++++++++

import datetime
from datetime import date
#import dataclasses

# New DataClass
class NewDataClass(type):
    def __new__(cls, name, bases, dct):
        final = super().__new__(cls, name, bases, dct)
        # pdb - The Python Debugger ; set_trace() adds a breakpoint
        # import pdb; pdb.set_trace()

        defaults = {
            key: value
            for key,value in final.__dict__.items()
            if not key.startswith("_")
        }

        #for key,value in final.__dict__.items():
        #    print(f"key: {key}, value: {value}")

        print(f"\ndefaults: {defaults}\n")

        init = final.__create_init(final.__annotations__, defaults)

        def eq(self, other):
            return self.__dict__ == other.__dict__

        def ne(self, other):
            return self.__dict__ != other.__dict__

        setattr(final, "__init__", init)
        setattr(final, "__eq__", eq)
        setattr(final, "__ne__", ne)

        return final

    @staticmethod
    def __create_init(annotations, defaults, *, return_type=None):
        name = "__init__"
        args = ["self"]
        default_args = []
        body_lines = []
        for key,value in annotations.items():
            print(f"annotations.items(): key: {key}, value: {value}, value.__name__: {value.__name__}")
            if key in defaults.keys():
                default_args.append(f"{key}:{value.__name__}={repr(defaults[key])}")
                print(f"\ndefault:  {key}:{value.__name__}={repr(defaults[key])}\n")
            else:
                args.append(f"{key}:{value.__name__}")
                # print(f"{key}:{value.__name__}")
            body_lines.append(f"self.{key} = {key}")

        args = ", ".join(args + default_args)
        body = "\n ".join(body_lines)

        text = f"def {name}({args})->{return_type}:\n {body}"
        # create function with 'exec()'
        # print("Debug 'text'")
        # print(text)
        # print("\n")
        exec(text)
        return locals()[name]

class Record(metaclass=NewDataClass):
    # alternative for setting metaclass:
    # __metaclass__ = NewDataClass
    artist: str
    title: str
    release_date: datetime.date = datetime.date.today()
    album_color: str = "blue"

# Note: print of 'record*' does not work because __repr__ was not implemented in NewDataClass
record1 = Record("The Beatles", "With the Beatles", datetime.date(1963, 11, 22), "red")
#print (f"\nrecord1:  {record1}")
record2 = Record("The Beatles", "With the Beatles", datetime.date(1963, 11, 22), "red")
#print (f"record2:  {record2}")
record3 = Record("The Beatles", "Help", datetime.date(1963, 11, 22))
#print (f"record3:  {record3}")

print (f"\nrecord1.artist:       {record1.artist}")
assert record1.artist == "The Beatles"
print (f"record1.title:        {record1.title}")
assert record1.title == "With the Beatles"
print (f"record1.album_color:  {record1.album_color}")
assert record1.album_color == "red"
print (f"record1.release_date: {record1.release_date}")
assert record1.release_date == datetime.date(1963, 11, 22)

# was not able to get below assert to work because it set "release_date: date: datetime.date.today()"
#   instead of  "release_date: datetime.date: datetime.date.today()"
#assert record3.release_date == datetime.date.today()

print (f"\nrecord3.album_color:  {record3.album_color}")
assert record3.album_color == "blue"
assert record1 == record2
assert record1 != record3


+++++++++++++++++++++++++++++++++++++++++++


# run  'record_newdataclass.py'
#   Note: comment the line 10:     # import pdb; pdb.set_trace()
$ python demo_code/9_3_type_class/record_newdataclass.py

$ python demo_code/9_3_type_class/record_newdataclass.py

  defaults: {'release_date': datetime.date(2025, 4, 2), 'album_color': 'blue'}

  annotations.items(): key: artist, value: <class 'str'>, value.__name__: str
  annotations.items(): key: title, value: <class 'str'>, value.__name__: str
  annotations.items(): key: release_date, value: <class 'datetime.date'>, value.__name__: date

  default:  release_date:date=datetime.date(2025, 4, 2)

  annotations.items(): key: album_color, value: <class 'str'>, value.__name__: str

  default:  album_color:str='blue'


  record1.artist:       The Beatles
  record1.title:        With the Beatles
  record1.album_color:  red
  record1.release_date: 1963-11-22

  record3.album_color:  blue


+++++++++++++++++++++++++++++++++++++++++++

++++++++
code in: demo_code/9_3_type_class/record_dataclass.py

   Notes: 
        - create record1/record2/record3, this time using the dataclasses.dataclass decorator 
        - I implemented to understand using builtin @dataclass decorator
       
+++++++++++++++++++++++++++++++++++++++++++
import datetime
from datetime import date
from dataclasses import dataclass

@dataclass
class Record():
    artist: str
    title: str
    release_date: datetime.date
    album_color: str = "blue"

record1 = Record("The Beatles", "With the Beatles", f"{datetime.date(1963, 11, 22)}", "red")
print (f"\nrecord1:  {record1}")
record2 = Record("The Beatles", "With the Beatles", f"{datetime.date(1963, 11, 22)}", "red")
print (f"record2:  {record2}")
record3 = Record("The Beatles", "Help", f"{datetime.date(1963, 11, 22)}")
print (f"record3:  {record3}")

print (f"\nrecord1.artist:       {record1.artist}")
assert record1.artist == "The Beatles"
print (f"record1.title:        {record1.title}")
assert record1.title == "With the Beatles"
print (f"record1.album_color:  {record1.album_color}")
assert record1.album_color == "red"
print (f"record1.release_date: {record1.release_date}")
assert record1.release_date == f"{datetime.date(1963, 11, 22)}"

# was not able to get below assert to work because it set "release_date: date: datetime.date.today()"
#   instead of  "release_date: datetime.date: datetime.date.today()"
#assert record3.release_date == datetime.date.today()

print (f"\nrecord3.album_color:  {record3.album_color}")
assert record3.album_color == "blue"
assert record1 == record2
assert record1 != record3


+++++++++++++++++++++++++++++++++++++++++++

# run  'record_dataclass.py'
$ python demo_code/9_3_type_class/record_dataclass.py

  # output
  record1:  Record(artist='The Beatles', title='With the Beatles', release_date='1963-11-22', album_color='red')
  record2:  Record(artist='The Beatles', title='With the Beatles', release_date='1963-11-22', album_color='red')
  record3:  Record(artist='The Beatles', title='Help', release_date='1963-11-22', album_color='blue')

  record1.artist:       The Beatles
  record1.title:        With the Beatles
  record1.album_color:  red
  record1.release_date: 1963-11-22

  record3.album_color:  blue

+++++++++++++++++++++++++++++++++++++++++++


---------------------------------------------------
9.4 Abstract Classes and Metaprogramming Summary


  abc [module] - Abstract Base Classes
    https://docs.python.org/3/library/abc.html
    - The 'abc' module provides the infrastructure for defining abstract base classes (ABCs) in Python

  Abstract Base Class Glossary entry
    https://docs.python.org/3/glossary.html#term-abstract-base-class

  What are Abstract Classes?
    - Abstract base classes introduce virtual subclasses, which are classes that don't inherit from a
      class but are still recognized by isinstance() and issubclass()
    - not a concrete class - works alot like an interface from other programming languages

  Abstract Classes
    - way to create a base class that requires subclasses to implement specified methods, class methods,
      and/or static methods
    - abstract base class is used to enforce polymorphism in the objects that you want to accept.
    - that is, it enforce the inheriting class to implement specified methods (via @abstractmethod),
      classmethods (via @abstractclassmethods, and staticmethods (via @abstractstaticmethods)

    Example Abstract Class
      Code in "demo_code/9_1_abstract_classes/abstract.py"

        from abc import ABC, abstractmethod, abstractclassmethod, abstractstaticmethod

        class APIClient(ABC):
            def __init__(self, domain):
                self.domain = domain

            @abstractmethod
            def get(self, url):
                pass

            @abstractclassmethod
            def default_headers(cls):
                pass

            @abstractstaticmethod
            def static_option():
                pass

        class MyClient(APIClient):
            pass

        # if you try to create an "MyClient", you get because 'MyClient' must first implement:
           # 'get' method, 'default_headers' classmethod, and 'static_option' staticmethod
        $ python -i demo_code/9_1_abstract_classes/abstract.py
        >>> client = MyClient("https://google.com")
        Traceback (most recent call last):
          File "<stdin>", line 1, in <module>
        TypeError: Can't instantiate abstract class MyClient with abstract methods default_headers, get, static_option


  Metaprogramming

    All about Metaclasses
      - what is the type of class itself?

       The 'type' class
         - every class in python is an instance of the 'type' class

       Metaclasses
         - 'type' defines how a class is created and is the default metaclass for all classes in python

       Custom Metaclasses
         - we can customize how a class is made by defining and using a new metaclass

    Creating a Class

      class Foo:
          def __init__(self, name):
              self.name = name

          def print_name(self):
              print(self.name)

    Creating a Class Dynamically

      - 'type' constructer can handle 3 args, a 'name', 'base classes', and 'attributes' (passed in via a 'dict')

      Example dynamically created Class
        Code in "demo_code/9_4_abstract_classes_and_metaprogramming/foo_dynamic.py"

        def foo_init(self, name):
            self.name = name

        def foo_print_name(self):
            print(self.name)

        Foo = type("Foo", (), {
            "__init__" : foo_init,
            "print_name" : foo_print_name,
        })


        foo_fred = Foo("Fred")
        print (f"foo_fred.name: {foo_fred.name}")
        print (f"foo_fred.print_name(): ")
        foo_fred.print_name()

        run: 
        $ python demo_code/9_4_abstract_classes_and_metaprogramming/foo_dynamic.py
          # output
          foo_fred.name: Fred
          foo_fred.print_name():
          Fred


    Creating a Custom Metaclass

      - must pass in "custom class" to subclass using 'metaclass' keyword

        Code in "demo_code/9_4_abstract_classes_and_metaprogramming/mymeta.py"

       class MyMeta(type):
           def __new__(cls, name, bases, dct):
               final = super().__new__(cls, name, bases, dct)

               # modify class here
               return final

       # inherit from 'custom class' by assigning 'metaclass' keyword to the 'custom class' (e.g. MyMeta)
       class MyClass(metaclass=MyMeta):
           pass


  Why create Metaclasses?

    1 Code Generation
      - the default metaclass builds every class the same way
      - But a custom metaclass could dynamically generate functions for a class

    2 That's About it
      - you will almost never need to use metaprogramming because most of what it would be used for can be
        done with a decorator or inheritance

---------------------------------------------------
datacamp: Introduction to Python Metaclasses
  https://www.datacamp.com/tutorial/python-metaclasses


  Creating Custom Metaclasses
   - customize the class creation process by passing the metaclass keyword in the class definition. 
   - This can also be done by inheriting a class that has already passed in this keyword.

     class MyMeta(type):
         pass

     class MyClass(metaclass=MyMeta):
         pass

     class MySubclass(MyClass):
         pass


   - We can see below that the type of MyMeta class is type and that the type of MyClass and MySubClass is MyMeta.

     print(type(MyMeta))
     print(type(MyClass))
     print(type(MySubclass))


     <class 'type'>
     <class '__main__.MyMeta'>
     <class '__main__.MyMeta'>

   - When defining a class and no metaclass is defined the default 'type' metaclass will be used. 
   - If a metaclass is given and it is not an instance of type(), then it is used directly as the metaclass.

  __new__ and __init__

    - Metaclasses can also be defined in one of the two ways shown below. 

      class MetaOne(type):
          def __new__(cls, name, bases, dict):
              pass

      class MetaTwo(type):
          def __init__(self, name, bases, dict):
              pass

    -  __new__ is used when one wants to define dict or bases tuples before the class is created. 
    - The return value of __new__is usually an instance of cls. __new__ allows subclasses of immutable types to 
      customize instance creation. 
    - It can be overridden in custom metaclasses to customize class creation. 

    - __init__ is usually called after the object has been created so as to initialize it.

  Metaclass __call__ method

    - According to the official docs, we can also override other class methods by defining a custom __call__() 
      method in the metaclass that allows custom behavior when the class is called.

  Metaclass __prepare__ method

     - Once the appropriate metaclass has been identified, then the class namespace is prepared. 
     - If the metaclass has a __prepare__ attribute, it is called as namespace = metaclass.__prepare__(name, bases, **kwds) 
       (where the additional keyword arguments, if any, come from the class definition). 
     - If the metaclass has no __prepare__attribute, then the class namespace is initialized as an empty ordered mapping. 

   Singleton Design using a Metaclass

     - This is a design pattern that restricts the instantiation of a class to only one object. 
     - This could prove useful for example when designing a class to connect to the database. 
     - One might want to have just one instance of the connection class.

       class SingletonMeta(type):
           _instances = {}
           def __call__(cls, *args, **kwargs):
               if cls not in cls._instances:
                   cls._instances[cls] = super(SingletonMeta,cls).__call__(*args, **kwargs)
               return cls._instances[cls]
       
       class SingletonClass(metaclass=SingletonMeta):
           pass
  
---------------------------------------------------
9.5 Utilizing Metaprogramming in Python - Lab

Learning objectives
  - Create ModelClass Metaclass and Add a Dynamic Initializer
  - Build and Attach get_by_xxx Class Methods for Each Attribute on the Model


Additional Resources

Your team is considering writing their own object relational mapper, and they've asked you to start by creating an
easy way for them to create a class and have it automatically have "getter" class methods to search the database
based on properties. Utilizing annotations for fields, they'd like to have class methods like get_by_name added to
the class if there was a class annotation for name: str. Here is an example:

class Post(metaclass=ModelClass):

    # if not defined, it would default to `post`.

    # For a multi-word class it would default to all lowercase, snake_cases

    # Eg. BlogPost would default to blog_post table.

    __tablename__ = "posts"



    title: str

    content: str

    published: bool



posts_query = Post.get_by_title("Example")

posts_query == "select * from posts where title = 'Example'"



new_post = Post("New Title", "This is my content", False)

To achieve this, we'll need to have the metaclass create an initializer based on the annotations and also
add get_by_xxx methods based on these same annotations. Because we're not working with an actual database yet,
we're just going to have these functions return the query. This query will generally follow the same patterns:

select * from [__tablename__] where [field_name] = value

There are a lot of things to consider when creating an ORM, but your job is to work on creating a good model
metaclass to make creating a new model incredibly easy.

There are some automated tests for this process. You can run all of the unit tests with the following command:

$  python -m unittest test_models.TestModelClass

...

----------------------------------------------------------------------

Ran 3 tests in 0.004s



OK

Learning Objectives

Create ModelClass Metaclass and Add a Dynamic Initializer

   Create a subclass of the type class and implement the __new__ method. From here, create and attach an initializer
   based on the annotations on the class.

Build and Attach get_by_xxx Class Methods for Each Attribute on the Model

   For each of the annotations on the class build a get_by_xxx method and attack it as a class method.

++++++++++
code in: labs/9_5_metaprogramming_lab/models.py
+++++++++++++++++++++++++++++++++++++++
import re

class ModelClass(type):
    def __new__(cls, name, based, dct):
        final = super().__new__(cls, name, based, dct)

        defaults = {
            key: value
            for key, value in final.__dict__.items()
            if not key.startswith("_")
        }

        init = final.__create_init(final.__annotations__, defaults)

        if not final.__dict__.get("__tablename__"):
            # print(f"final.__snake_case(final.__name__): {final.__snake_case(final.__name__)}")
            setattr(final, "__tablename__", final.__snake_case(final.__name__))

        tablename = final.__dict__["__tablename__"]

        setattr(final, "__init__", init)

        for field_name, field_type, in final.__annotations__.items():
            (func_name, func) = final.__create_get_by(tablename, field_name, field_type)
            setattr(final, func_name, func)

        return final

    @staticmethod
    def __create_init(annotations, defaults, return_type=None):
        name = "__init__"
        args =["self"]
        default_args = []
        body_lines = []

        for key, value in annotations.items():
            if key in defaults.keys():
                default_args.append(f"{key}:{value.__name__}={repr(defaults[key])}")
            else:
                args.append(f"{key}:{value.__name__}")

            body_lines.append(f"self.{key} = {key}")

        args = ", ".join(args + default_args)
        body = "\n ".join(body_lines)

        text = f"def {name}({args})->{return_type}:\n {body}"
        # run the code to create the method
        exec(text)
        return locals()[name]

    @staticmethod
    def __snake_case(value):
        # change from "OtherExample" -> _other_example -> other_example
         return re.sub("([A-Z])", r"_\1", value).lower().strip("_")

    @staticmethod
    def __create_get_by(tablename, field_name, field_type):
        base_query = f"select * from {tablename} where {field_name} = "
        body = None

        name = f"get_by_{field_name}"

        if field_type == str:
            body = f'value = value.replace("\'", "\'\'")\n return f"{base_query}\'{{value}}\'"'
        elif field_type == bool:
            body = f"value = 'true' if value else 'false'\n return f\"{base_query}{{value}}\""
        else:
            body = f' return f"{base_query}\'{{value}}\'"'

        text = f"def {name}(value):\n {body}"
        # print(f"\ntext:\n{text}\n")
        exec(text)
        # print (f"\nname: {name}, locals()[name]: {locals()[name]}\n")
        return (name, locals()[name])


if __name__ == '__main__':
    class Post(metaclass=ModelClass):
        __tablename__ = "posts"
        title: str
        content: str
        published: bool

    class OtherExample(metaclass=ModelClass):
        title: str
        content: str
        published: bool

    post = Post("New Title", "This is my content", False)
    print(f"post.__tablename__: {post.__tablename__} \t\t\texpected value: 'posts'")
    print(f"post.title:         {post.title} \t\t\texpected value: 'New Title'")
    print(f"post.content:       {post.content} \t\texpected value: 'This is my content'")
    print(f"post.published:     {post.published} \t\t\texpected value: 'False'")

    print(f"OtherExample.__tablename__: {OtherExample.__tablename__} \texpected value: 'other_example'")


    print("\ninfo = dir(Post)")
    info = dir(Post)
    print(f'"get_by_title" in info: {"get_by_title" in info} \t\t\texpected value: "True"')
    print(f'"get_by_content" in info: {"get_by_content" in info} \t\t\texpected value: "True"')
    print(f'"get_by_published" in info: {"get_by_published" in info} \t\texpected value: "True"')
    print(f'\nPost.get_by_title("My Title")         {Post.get_by_title("My Title")}')
    print('                     expected value:  select * from posts where title = \'My Title\'')
    print(f'\nPost.get_by_content("Some content")   {Post.get_by_content("Some content")}')
    print('                     expected value:  select * from posts where content = \'Some content\'')
    print(f'\nPost.get_by_published(False)          {Post.get_by_published(False)}')
    print('                     expected value:  select * from posts where published = false')

+++++++++++++++++++++++++++++++++++++++

# run "models.py"

$ python labs/9_5_metaprogramming_lab/models.py
  # output
  post.__tablename__: posts                       expected value: 'posts'
  post.title:         New Title                   expected value: 'New Title'
  post.content:       This is my content          expected value: 'This is my content'
  post.published:     False                       expected value: 'False'
  OtherExample.__tablename__: other_example       expected value: 'other_example'
  
  info = dir(Post)
  "get_by_title" in info: True                    expected value: "True"
  "get_by_content" in info: True                  expected value: "True"
  "get_by_published" in info: True                expected value: "True"
  
  Post.get_by_title("My Title")         select * from posts where title = 'My Title'
                       expected value:  select * from posts where title = 'My Title'
  
  Post.get_by_content("Some content")   select * from posts where content = 'Some content'
                       expected value:  select * from posts where content = 'Some content'
  
  Post.get_by_published(False)          select * from posts where published = false
                       expected value:  select * from posts where published = false


+++++++++++++++++++++++++++++++++++++++

++++++++++
code in: labs/9_5_metaprogramming_lab/test_models.py (provided)
+++++++++++++++++++++++++++++++++++++++

import unittest

from models import ModelClass


class Post(metaclass=ModelClass):
    __tablename__ = "posts"

    title: str
    content: str
    published: bool


class OtherExample(metaclass=ModelClass):
    title: str
    content: str
    published: bool


class TestModelClass(unittest.TestCase):
    def test_generated_init(self):
        post = Post("New Title", "This is my content", False)

        self.assertEqual(post.__tablename__, "posts")
        self.assertEqual(post.title, "New Title")
        self.assertEqual(post.content, "This is my content")
        self.assertEqual(post.published, False)

    def test_default_tablename(self):
        self.assertEqual(OtherExample.__tablename__, "other_example")

    def test_generated_get_by_methods(self):
        info = dir(Post)
        self.assertTrue("get_by_title" in info)
        self.assertTrue("get_by_content" in info)
        self.assertTrue("get_by_published" in info)

        self.assertEqual(
            Post.get_by_title("My Title"),
            "select * from posts where title = 'My Title'",
        )
        self.assertEqual(
            Post.get_by_content("Some content"),
            "select * from posts where content = 'Some content'",
        )
        self.assertEqual(
            Post.get_by_published(False), "select * from posts where published = false"
        )


+++++++++++++++++++++++++++++++++++++++
run test_models.py

$ cd labs/9_5_metaprogramming_lab
$ python -m unittest test_models.TestModelClass
  ...
  ----------------------------------------------------------------------
  Ran 3 tests in 0.001s

  OK
 
+++++++++++++++++++++++++++++++++++++++


---------------------------------------------------

Chapter 10 Advanced Expections

---------------------------------------------------
10.1 Exceptions Overview

  Exception Attributes
    1 Exceptions are Normal Classes
      - realizing this, we can create exceptions that know more about the scenarios when they occur
      - e.g. can be defined with their own initializers with parameters passed in

    2 Custom Parameters are Great
      - Being able to catch an exception and get useful information from it besides just a name is incredibly helpful

    3 Built-in Exceptions Have Attributes too
      - take a look at "ImportError' to see some of the custom attributes that it has

  Exception Chaining
    - added to a python 3+ version
    - occurs when an exception occurs within an exception

  Utilizing Traceback
    - traceback is a python concept, while stack trace is common to most/all programming languages
    - traceback is stack trace backwards
    - example showing accessing traceback as a string while handling the exception (code in 'demo_code/10_1_exceptions/bad_math.py':

        import traceback

        def bad_math():
            try:
                10 / 0
            except ZeroDivisionError as err:
                # access traceback as a string while still handling the exception
                tb = traceback.format_tb(err.__traceback__)
                print(tb)

      # output:
      $ python -i demo_code/10_1_exceptions/bad_math.py
      >>> bad_math()
      ['  File "\\demo_code/10_1_exceptions\\bad_math.py", line 5, in bad_math\n    10 / 0\n']

---------------------------------------------------
10.2 Working with Exception Attributes

  Errors and Exceptions handling doc:
    https://docs.python.org/3/tutorial/errors.html

  Exception Hierarchy:
    https://docs.python.org/2/library/exceptions.html#exception-hierarchy

  Python Exceptions

    Exception Syntax:
      https://www.geeksforgeeks.org/python-exception-handling/

      try:
          # Some Code....
      except <ErrorType1>:
          # optional block
          # Handling of exception (if required)
      else:
          # execute if no exception
      finally:
          # Some code .....(always executed)

    Exception code Example:

      try:
          k = 5//0
          print(k)
      except ZeroDivisionError:
          print("Can't divide by zero")
      finally:
          print('This is always executed')


    Raise an exception
      https://www.w3schools.com/python/gloss_python_raise.asp
      - To throw (or raise) an exception, use the 'raise' keyword.

     Raise an Exception examples:
      1. Raise an error and stop the program if x is lower than 0:

        x = -1
        if x < 0:
          raise Exception("Sorry, no numbers below zero")

      2. Raise a TypeError if x is not an integer:

        x = "hello"
        if not type(x) is int:
          raise TypeError("Only integers are allowed")


  Create a custom Exception class (for handling StateMachine transition errors):
     - Code in "demo_code/10_2_exception_attributes/transition_error.py"

        class TransitionError(Exception):
            """Raised when operation attempts a state transition that's not allowed

            Attributes:
                previous   -- state at beginning of transition
                next_state -- attempt new state
                message    -- explanation of why the specific transition is not allowed
            """

            def __init__(self, previous, next_state, message) -> None:
                self.previous = previous
                self.next = next_state
                self.message = message

   Create a StateMachine class that using the TransitionError class for error handling
     - code in "demo_code/10_2_exception_attributes/state_transition.py"

        from transition_error import TransitionError

        class StateMachine:
            allowed_transitions = {
                "new": ["loading"],
                "loading": ["completed", "incomplete"],
                "incomplete": [],
                "completed": ["cancelled"]
            }

            def __init__(self, state="new") -> None:
                self.state = state

            def transition(self, new_state):
                if new_state.lower() in self.allowed_transitions[self.state]:
                    self.state = new_state.lower()
                else:
                    raise TransitionError(self.state, new_state, f"Unable to transition from {self.state} to {new_state}. Can only transition to {self.allowed_transitions[self.state]}")

        if __name__ == "__main__":
            sm = StateMachine()
            try:
                sm.transition("loading")
                sm.transition("cancelled")
            except TransitionError as err:
                print(f"Previous State: {err.previous}")
                print(f"Desired State: {err.next}")
                print(err.message)


        # output - error handling when trying to illegally transition from 'loading' state to 'cancelled' state
        $ python demo_code/10_2_exception_attributes/state_transition.py
        Previous State: loading
        Desired State: cancelled
        Unable to transition from loading to cancelled. Can only transition to ['completed', 'incomplete']


---------------------------------------------------
10.3 Chaining Exceptions

  - Exception chaining is handling an exception while processing/handling another exception
  - Exception chaining: exceptions thrown while exception handing

  Exception Chaining
    https://docs.python.org/3/tutorial/errors.html#exception-chaining


  Example of implicit exception chaining:
     - code in "demo_code/10_3_chaining_exceptions/implicit_chaining.py"

        # calling my_func will throw (raise) and IOError
        def my_func():
            raise IOError

        def implicit_chaining():
            try:
                my_func()
            except IOError as err:
                # this will cause a 2nd expection during the 'my_func()' IOError exception handling
                1 / 0

        implicit_chaining()

        $ python demo_code/10_3_chaining_exceptions/implicit_chaining.py
          # output
          Traceback (most recent call last):
            File "demo_code\10_3_chaining_exceptions\implicit_chaining.py", line 7, in implicit_chaining
              my_func()
            File "demo_code\10_3_chaining_exceptions\implicit_chaining.py", line 3, in my_func
              raise IOError
          OSError
          
          During handling of the above exception, another exception occurred:
          
          Traceback (most recent call last):
            File "demo_code\10_3_chaining_exceptions\implicit_chaining.py", line 12, in <module>
              implicit_chaining()
            File "demo_code\10_3_chaining_exceptions\implicit_chaining.py", line 10, in implicit_chaining
              1 / 0
          ZeroDivisionError: division by zero

  Example of explicit exception chaining:
     - code in "demo_code/10_3_chaining_exceptions/explicit_chaining.py"

        def my_func():
            raise IOError

        def explicit_chaining():
            try:
                my_func()
            except IOError as err:
                raise ValueError("unexpected value") from err

        explicit_chaining()

        $ python demo_code/10_3_chaining_exceptions/explicit_chaining.py
          # output
          Traceback (most recent call last):
            File "demo_code\10_3_chaining_exceptions\explicit_chaining.py", line 8, in explicit_chaining
              my_func()
            File "demo_code\10_3_chaining_exceptions\explicit_chaining.py", line 4, in my_func
              raise IOError
          OSError
          
          The above exception was the direct cause of the following exception:
          
          Traceback (most recent call last):
            File "demo_code\10_3_chaining_exceptions\explicit_chaining.py", line 12, in <module>
              explicit_chaining()
            File "demo_code\10_3_chaining_exceptions\explicit_chaining.py", line 10, in explicit_chaining
              raise ValueError("unexpected value") from err
          ValueError: unexpected value


  Example of nested exception chaining with pdb (python debugger):
     - code in "demo_code/10_3_chaining_exceptions/nested_chaining.py"

        def my_func():
            raise IOError

        def explicit_chaining():
            try:
                my_func()
            except IOError as err:
                raise ValueError("unexpected value") from err

        def nested():
            try:
                explicit_chaining()
            except ValueError as err:
                print()
                import pdb; pdb.set_trace()
                print()

        nested()

        # output
        $ python demo_code/10_3_chaining_exceptions/nested_chaining.py
          > demo_code\10_3_chaining_exceptions/nested_chaining.py(17)nested()
          -> print()
          (Pdb) err
            ValueError('unexpected value')
          (Pdb) err.__context__
            OSError()
          (Pdb) err.__cause__                     <-- 'cause' was reported due to "raise .... from err"
            OSError()
          (Pdb) --KeyboardInterrupt--
          (Pdb)


  Example with multiple exceptions 
    - when specifying multiple exceptions, the first expection that is valid is triggered
    - in this example, both "OSError" exception and the generic "Exception" are true, but OSError is triggerred 
      because it is checked first
     - code in "demo_code/10_3_chaining_exceptions/exp.py"

        import sys

        try:
            f = open('myfile.txt')
            s = f.readline()
            i = int(s.strip())
        except ValueError:
            print("Could not convert data to an integer.")
        except OSError as err:
            print(f"OS error:   err: {err}")
        except Exception as err:
            print(f"Unexpected {err=}, {type(err)=}")
            raise


        $ python exp.py
          # output
          OS error:   err: [Errno 2] No such file or directory: 'myfile.txt'


  ----------------------
  Python - Exception Chaining
    https://www.tutorialspoint.com/python/python_exception_chaining.htm

    The __context__ and __cause__ Expression
      - Raising an exception in the except block will automatically add the captured exception to the __context__ attribute of the new exception. 
      - Similarly, you can also add __cause__ to any exception using the expression raise ... from syntax.

      Example:
        try:
           try:
              raise ValueError("ValueError")
           except ValueError as e1:
              raise TypeError("TypeError") from e1
        except TypeError as e2:
           print("The exception was", repr(e2))
           print("Its __context__ was", repr(e2.__context__))
           print("Its __cause__ was", repr(e2.__cause__))

        - It will produce the following output −

          The exception was TypeError('TypeError')
          Its __context__ was ValueError('ValueError')
          Its __cause__ was ValueError('ValueError')
  ----------------------

---------------------------------------------------
10.4 Working with Traceback

  the traceback module
    https://docs.python.org/3/library/traceback.html

  Example of explicit nested exception chaining with traceback
     - code in "demo_code/10_4_traceback/explicit_nested_chaining.py"

        from traceback import format_tb, format_stack

        def my_func():
            raise IOError

        def explicit_chaining():
            try:
                my_func()
            except IOError as err:
                raise ValueError("unexpected value") from err

        def explicit_nested():
            try:
                explicit_chaining()
            except ValueError as err:
                traceback = format_tb(err.__traceback__)
                stacktrace = format_stack()
                with open("traceback.txt", "w") as f:
                    f.write("\n".join(traceback))

                with open("stracktrace.txt", "w") as f:
                    f.write("\n".join(stacktrace))

                print("ValueError caught here")

        explicit_nested()

        $ python demo_code/10_4_traceback/explicit_nested_chaining.py
          # output
          ValueError caught here

        $ cat traceback.txt
          File "demo_code\10_4_traceback\explicit_nested_chaining.py", line 20, in explicit_nested
            explicit_chaining()

          File "demo_code\10_4_traceback\explicit_nested_chaining.py", line 16, in explicit_chaining
            raise ValueError("unexpected value") from err

        $ cat stracktrace.txt
          File "demo_code\10_4_traceback\explicit_nested_chaining.py", line 32, in <module>
            explicit_nested()

          File "demo_code\10_4_traceback\explicit_nested_chaining.py", line 23, in explicit_nested
            stacktrace = format_stack()


---------------------------------------------------

Chapter 11 Object Serialization

---------------------------------------------------
11.1 Overview of Python Object Serialization

  The pickle Module
    - allows saving objects for later

    Serialize
      - turn python objects into bytes of data
      - actual Python objects (e.g. instances of our own custom classes, integers, dictionaries, etc.,) into bytes that can then
        written into a file, and then we can also transport them over a wire or put them in a database or do any of those things.
    Transport
      - bytes can be sent over the wire or put in a database
    Deserialize
      - take pickled bytes and rehydrate them as python objects

    Example Use Case - a background queuing system
      - a background queuing system that runs on a completely different system than where the main system is -
        you share access to the same database, you have access to the same code, but you want that other system to
        be working way to queue, but you're going to serialize your stuff, write it into the queue,
        and then it's going to read it back out later on.

  Shelve Key-Value Store
    1 A Database for Pickled Objects
      - the shelve module is module built on top of the pickle that allows us to read and write pickled
        objects to be a flat file key-value store
    2 Good for Simple Data Storage
      - when we don't need a full-fledge database, 'shelve' can be a good option to allow us to persist
        data for our application

---------------------------------------------------
11.2 Using Pickle

  The pickle module
    https://docs.python.org/3/library/pickle.html

  What can be pickled and unpickled?
    https://docs.python.org/3/library/pickle.html#pickle-picklable

  Example of creating a pickle file (people.pickle) containing list of custom "Person' objects
    - code in "demo_code/11_2_pickle/serial.py"

        import pickle
        from datetime import date

        class Person:
            def __init__(self, name: str, birthdate: date) -> None:
                self.name = name
                self.birthdate = birthdate

        person1 = Person("Kevin Bacon", date(1958, 7, 8))
        person2 = Person("Other Person", date.today())

        # create list of persons:
        people = [person1, person2]

        # open pickle file in write byte mode and write list of Person objects to file
        with open("people.pickle", "wb") as f:
            # pickle module has same API as the json module (e.g. dump, dumps, load, loads)
            # dump 'people' list of objects to a file
            pickle.dump(people, f)

  Run "demo_code/11_2_pickle/serial.py"
    $ python demo_code/11_2_pickle/serial.py
    $ ls -l people.pickle
    -rwxr-xr-x+ 1 pat None 152 Dec  4 13:22 people.pickle
    $ file !$
    file people.pickle
    people.pickle: data

  Example of reading a pickle file ('people.pickle') containing list of custom "Person' objects
    - code in "demo_code/11_2_pickle/loading.py"

        import pickle
        from serial import Person

        with open ("people.pickle", "rb") as f:
            people = pickle.load(f)

        for person in people:
            print(type(person))
            print(person.__dict__)

  Run "demo_code/11_2_pickle/loading.py"
    $ python demo_code/11_2_pickle/loading.py
    <class 'serial.Person'>
    {'name': 'Kevin Bacon', 'birthdate': datetime.date(1958, 7, 8)}
    <class 'serial.Person'>
    {'name': 'Other Person', 'birthdate': datetime.date(2023, 12, 4)}

----------- 
Extra stuff - dict to str file to str to dict

  Example : writing student dict to file (str), reading (str) file, converting str to dict
    - code in "demo_code/11_2_pickle/students_dict_to_str_file.py"

        import json
        import os

        students_dict = {

          'Student 1': {
                'Name': "Alice", 'Age' :10, 'Grade':4,
            },
            'Student 2': {
                'Name':'Bob', 'Age':11, 'Grade':5
            },
            'Student 3': {
                'Name':'Elena', 'Age':14, 'Grade':8
            }
        }


        print (f"\ntype(students_dict): {type(students_dict)}")

        filename = "students.txt"

        # if needed clean-up  / rm filename
        if os.path.exists(filename):
            os.remove(filename)
            print(f"File '{filename}' deleted successfully.")

        print (f"\nWrite students_dict dict as str to {filename}")
        try:
            with open(filename,'w') as data:
                  data.write(str(students_dict))
        except PermissionError:
            print (F"Error: file permission error occurred with writing {filename}")
        else:
            print (f"\nRead {filename} contents")

        print (f"\nRead and print {filename} contents")
        try:
            with open(filename, 'r') as f:
                students_str = f.read()
                #for students_str in f:
                #    print(students_str)
        except FileNotFoundError:
            print(f"Error: File not found: {filename}")
        except IOError:
            print(f"Error: An error occurred while reading {filename}")
        else:
            print(f"\nstudents_str (str from file):\n {students_str}\n")
            print (f"\ntype(students_str): {type(students_str)}\n")


        print (f"Convert students_str to dict")
        try:
            #students_dict = json.loads(students_str.replace("'", """))
            students_dict = eval(students_str)
        #except json.JSONDecodeError:
        except Exception:
            print(f"Error: Invalid format - student_str cannot be converted to students_dict")
        else:
            print(f"\nstudents_dict (dict from str):\n {students_dict}\n")
            print (f"type(students_dict): {type(students_dict)}\n")



        $ python demo_code/11_2_pickle/students_dict_to_str_file.py
          # output

          type(students_dict): <class 'dict'>

          Write students_dict dict as str to students.txt

          Read students.txt contents

          Read and print students.txt contents

          students_str (str from file):
           {'Student 1': {'Name': 'Alice', 'Age': 10, 'Grade': 4}, 'Student 2': {'Name': 'Bob', 'Age': 11, 'Grade': 5}, 'Student 3': {'Name':
          'Elena', 'Age': 14, 'Grade': 8}}


          type(students_str): <class 'str'>

          Convert students_str to dict

          students_dict (dict from str):
           {'Student 1': {'Name': 'Alice', 'Age': 10, 'Grade': 4}, 'Student 2': {'Name': 'Bob', 'Age': 11, 'Grade': 5}, 'Student 3': {'Name':
          'Elena', 'Age': 14, 'Grade': 8}}

          type(students_dict): <class 'dict'>




----------- 
Extra stuff - dict to json file to dict

  Example : writing student dict to file (json), reading (json) file to dict
    - code in "demo_code/11_2_pickle/students_dict_to_json_file.py"

        import json
        import os

        students = {

          'Student 1': {
                'Name': "Alice", 'Age' :10, 'Grade':4,
            },
            'Student 2': {
                'Name':'Bob', 'Age':11, 'Grade':5
            },
            'Student 3': {
                'Name':'Elena', 'Age':14, 'Grade':8
            }
        }


        print (f"\ntype(students): {type(students)}")

        json_file = "students.json"

        # if needed clean-up  / rm json_file
        if os.path.exists(json_file):
            os.remove(json_file)
            print(f"File '{json_file}' deleted successfully.")

        print (f"\nWrite students dict as json to {json_file}")
        try:
            with open(json_file, 'w') as file:
              json.dump(students, file, indent=4,  sort_keys=True, separators=(',',':'))
        except PermissionError:
            print (F"Error: file permission error occurred with writing {json_file}")
        else:
            print (f"\nRead {json_file} contents")


        print (f"\nRead json from {json_file} to students dict")
        try:
            with open(json_file, 'r') as file:
                students = json.load(file)
        except FileNotFoundError:
            print(f"Error: File not found: {json_file}")
        except json.JSONDecodeError:
            print(f"Error: Invalid JSON format in: {json_file}")
        except IOError:
            print(f"Error: An error occurred while reading {json_file}")

        print(f"\nstudents (dict from json file):\n {students}\n")

        print (f"type(students): {type(students)}\n")



        $ python demo_code/11_2_pickle/students_dict_to_json_file.py

          type(students): <class 'dict'>

          Write students dict as json to students.json

          Read students.json contents

          Read json from students.json to students dict

          students (dict from json file):
           {'Student 1': {'Age': 10, 'Grade': 4, 'Name': 'Alice'}, 'Student 2': {'Age': 11, 'Grade': 5, 'Name': 'Bob'}, 'Student 3': {'Age': 1
          4, 'Grade': 8, 'Name': 'Elena'}}

          type(students): <class 'dict'>

----------- 
---------------------------------------------------
11.3 Using Shelve

  shelve module
  https://docs.python.org/3/library/shelve.html


  Example of shelf database file ("people.shelf") and writing to the DB file and reading the DB file
    - code in "demo_code/11_3_shelve/shelving.py"

        import shelve
        from datetime import date
        from serial import Person


        person1 = Person("Kevin Bacon", date(1958, 7, 8))
        person2 = Person("Other Person", date.today())

        # DB_NAME is the file name with flag='c' (Open database for reading and writing, creating it if it doesn’t exist)
        DB_NAME = "people.shelf"
        with shelve.open(DB_NAME, "c") as shelf:
            shelf["kevin"] = person1
            shelf["other"] = person2
            shelf["count"] = 2

        # re-open DB_NAME file, this time with flag='r' (Open existing database for reading only)
        with shelve.open(DB_NAME, "r") as shelf:
            print(f"Keys: {list(shelf.keys())}")
            print(f"Values: {list(shelf.values())}")

            print(f'\ntype(shelf["kevin"]):        {type(shelf["kevin"])}')
            print(f'shelf["kevin"].__dict__:     {shelf["kevin"].__dict__}')
            print(f'shelf["other"].__dict__:     {shelf["other"].__dict__}')

  Run "shelving.py"
    # Note: requires 'demo_code/11_2_pickle/serial.py' to be in same directory
    $ python shelving.py
    Keys: ['kevin', 'other', 'count']
    Values: [<serial.Person object at 0x0000027C632A13A0>, <serial.Person object at 0x0000027C632A1730>, 2]

    type(shelf["kevin"]):        <class 'serial.Person'>
    shelf["kevin"].__dict__:     {'name': 'Kevin Bacon', 'birthdate': datetime.date(1958, 7, 8)}
    shelf["other"].__dict__:     {'name': 'Other Person', 'birthdate': datetime.date(2023, 12, 5)}



  ---------
  Difference Between Shelve vs Pickle

    - The pickle module is used to convert  Python objects into a byte stream. 
    - Python objects like lists, dictionaries, etc are supported by it. 

    - The shelve module is built on top of the pickle module. 
    - It implements a serialization dictionary where keys are strings and values are pickled Python objects. 
    - Values can be only those objects that can be handled by the pickle module. 
    - The shelve module is used when we need to store data that is small in size and less complex. 
    - For large and complex data we use a database. 
    - It is insecure to load a shelf from an untrusted source because loading a shelf can execute arbitrary code.


  ---------
  AI Overview for: python shelve files created
    - When a shelve file is created in Python using the shelve module, the files created depend on the underlying database module used. 
    - The shelve module uses dbm which in turn might use different implementations depending on the operating system and available libraries.

    dumbdbm (Default on some systems):
        .dat: Stores the actual data.
        .dir: Stores the index (keys and their positions in the .dat file).
        .bak: A backup of the .dir file, created during updates.
     . . .
  ---------


---------------------------------------------------
11.4 Serializing Python Objects with Pickle and Shelve - Lab

  Object Serialization with Picke and Shelve
    - Object serialization and persistence can be incredibly useful if you need to share data between multiple
      applications or save data for use later.
    - Python provides utilities for serializing and deserializing Python objects to and from bytes, to make it
      possible to pass Python data structures between Python applications or to read/write to a file without needing
      to convert to an intermediate data format like JSON.


Learning objectives
  - Create a FlashCard Class with a Front and a Back
  - Implement new, delete, and list CLI Subcommands
  - Implement the practice Command


 install 'click'
   $ pip install -U click



Additional Resources

You're planning on taking a difficult certification and know that you need to study. You've decided that the best way for you to 
study is to use flashcards. To facilitate this, you've decided to build a flashcards tool for yourself that will allow you to create 
and delete flashcards, and also randomly present you with a variable number of questions to practice. You want to be able to keep 
your work between study sessions, so you want to persist your flashcards. You have decided to use either pickle or shelve from the 
Python standard library to do this. Here's how you've planned on having the CLI work:

    Adding a new flashcard

$ ./flashcards.py new "Front of card" "Back of card"

New card added to flashcard deck.

    Listing flashcards

$ ./flashcards.py list

1. Front of first card

2. Front of second card

    Deleting a flashcard

$ ./flashcards.py delete 1

Card Deleted: Front of first card

    Practicing

$ ./flashcards.py practice 5 # optional defaults to 10

Asking 5 questions.



Question 1. (press Enter to reveal back of cards):



Front of first card



Back of first card



Press Enter to see the next question.



Question 2. (press Enter to reveal back of cards):



Front of another card



Back of another card

The file flashcards.py is already executable and includes the boilerplate for building a CLI with subcommands. To finish this tool, 
you'll need to implement the logic of the subcommands within the various functions.


Create a FlashCard Class with a Front and a Back

The FlashCard class needs to hold onto some string information. This class is a data class.

Implement new, delete, and list CLI Subcommands

The new_flashcard, delete_flashcard, and list_flashcards functions all need to be implemented so that they can read and write from a local file that persists the flashcard information.

Implement the practice Command

When practicing, you want to be able to specify the number of questions.

++++++++++++++++++++
 code in: serialization_lab/flashcards.py  (partially provided)
+++++++++++++++++++++++++++++++++++++++++++++++++++++
#!/usr/bin/env python

from dataclasses import dataclass
from random import sample
import shelve
import click

@dataclass
class FlashCard:
    front: str
    back: str

DB_NAME = "flashcards.shlv"

@click.group()
def flashcards():
    pass

@click.command()
@click.argument("front")
@click.argument("back")
def new_flashcard (front, back):
    """Create a new flashcard

    FRONT is the question on the card
    BACK is the information on the back of the card
    """
    with shelve.open(DB_NAME, 'c') as shlv:
        card = FlashCard(front, back)
        cards = shlv.get("cards", [])
        cards.append(card)
        shlv["cards"] = cards

    print("New card added to flash card deck.")


@click.command()
def list_flashcards():
    """List flashcards by card number and card fronts.
    """
    with shelve.open(DB_NAME, 'c') as shlv:
        cards = shlv.get("cards", [])
        if cards:
            for index, card in enumerate(cards):
                print(f"{index + 1}. {card.front}")
        else:
            print("No cards in deck currently")

@click.command()
@click.argument("card_number", type=int)
def delete_flashcard(card_number):
    """Delete a flashcard

    CARD_NUMBER is the number displayed before the question when running the `list` command
    """
    with shelve.open(DB_NAME, 'c') as shlv:
        cards = shlv.get("cards", [])
        if card_number > 0 and card_number <= len(cards):
            card = cards.pop(card_number - 1)
            shlv["cards"] = cards
            print(f"Card Deleted: {card.front}")
        else:
            print(f"Unable to delete card number {card_number}")

@click.command()
@click.argument("number_of_cards", default=10, type=int)
def practice(number_of_cards):
    """Present flashcards to study

    NUMBER_OF_CARDS is the number of questions to ask. Default to 10
    """
    with shelve.open(DB_NAME, 'c') as shlv:
        cards = shlv.get("cards", [])
        if not cards:
            print("No flashcards created yet.")
            return

        if number_of_cards > len(cards):
            number_of_cards = len(cards)

        print(f"Asking {number_of_cards} questions.")
        questions_to_ask = sample(cards, number_of_cards)
        for index, question in enumerate(questions_to_ask):
            input(
                f"Question {index + 1}. (presss Enter to reveal the back of card):\n\n{question.front}\n"
            )
            print(question.back)
            if not index + 1 == number_of_cards:
                input("\nPress Enter to see the next question.")


flashcards.add_command(new_flashcard, "new")
flashcards.add_command(list_flashcards, "list")
flashcards.add_command(delete_flashcard, "delete")
flashcards.add_command(practice, "practice")

if __name__ == "__main__":
    flashcards()

+++++++++++++++++++++++++++++++++++++++++++++++++++++

Run:

    # create new flashcard where question front is "Front"
    $ cd labs/11_4_serialization_lab
    $ ./flashcards.py new "Front" "Back of card"
    New card added to flash card deck.

    # list current flashcards
    $ ./flashcards.py list
    1. Front

    # create another new flashcard where question front is "Front"
    $ ./flashcards.py new "Front" "Back of card"
    New card added to flash card deck.

    # list current flashcards
    $ ./flashcards.py list
    1. Front
    2. Front

    # list files
    $ ls -lrt
    total 11
    -rwxr-xr-x+ 1 pat None 2461 Dec  4 18:29 flashcards.py
    -rwxr-xr-x+ 1 pat None  141 Dec  4 18:29 flashcards.shlv.dat
    -rwxr-xr-x+ 1 pat None   19 Dec  4 18:29 flashcards.shlv.bak
    -rwxr-xr-x+ 1 pat None   19 Dec  4 18:29 flashcards.shlv.dir

    # delete 2nd card
    $ ./flashcards.py delete 2
    Card Deleted: Front

    # list current flashcards
    $ ./flashcards.py list
    1. Front

    # create new flashcard where question front is "Another example"
    $ ./flashcards.py new "Other card" "Another example"
    New card added to flash card deck.

    # create new flashcard where question front is "First Name"
    $ ./flashcards.py new "First Name" "Kevin"
    New card added to flash card deck.

    # list current flashcards
    $ ./flashcards.py list
    1. Front
    2. Other card
    3. First Name

    # practice with flashcards - will display 'front' and you enter to reveal 'back'
    # defaults to 3 cards since only 3 cards in deck
    $ ./flashcards.py practice
    Asking 3 questions.
    Question 1. (presss Enter to reveal the back of card):

    Other card

    Another example

    Press Enter to see the next question.
    Question 2. (presss Enter to reveal the back of card):

    Front

    Back of card

    Press Enter to see the next question.
    Question 3. (presss Enter to reveal the back of card):

    First Name

    Kevin

    # practice with 1st flashcard - will display 'front' and you enter to reveal 'back'
    $ ./flashcards.py practice 1
    Asking 1 questions.
    Question 1. (presss Enter to reveal the back of card):

    First Name

    Kevin

   ---------
  click 8.1.8
  https://pypi.org/project/click/

    to install:
      pip install click
   

    Project description
    $ click_

     - Click is a Python package for creating beautiful command line interfaces in a composable way with as little code as necessary. 
     - It's the "Command Line Interface Creation Kit". It's highly configurable but comes with sensible defaults out of the box.

     - It aims to make the process of writing command line tools quick and fun while also preventing any frustration caused by the 
       inability to implement an intended CLI API.

     Click in three points:
       - Arbitrary nesting of commands
       - Automatic help page generation
       - Supports lazy loading of subcommands at runtime

     A Simple Example

        import click
        
        @click.command()
        @click.option("--count", default=1, help="Number of greetings.")
        @click.option("--name", prompt="Your name", help="The person to greet.")
        def hello(count, name):
            """Simple program that greets NAME for a total of COUNT times."""
            for _ in range(count):
                click.echo(f"Hello, {name}!")
        
        if __name__ == '__main__':
            hello()
        
        $ python hello.py --count=3
        Your name: Click
        Hello, Click!
        Hello, Click!
        Hello, Click!
   

   ---------

---------------------------------------------------

Chapter 12 Conclusion

---------------------------------------------------
12.1 Course Summary

  Big Take-aways

  1 Don't be afraid of Dunder (double under) Methods
     - modifiy the data model of custom objects can be incredibly powerful and help to make useful
       objects even more useful

  2 Functions are objects
    - we've learned a lot about how are objects and being able to pass functions (or any callable around)
      can allow you to create awesome programs

  3 Just because you can ...
     - doesn't mean that you should
     - e.g. metaprogramming generally is not the recommended approach with some exceptions

---------------------------------------------------
12.2 Conclusion and What's Next

    - look into other 'with python' courses
    -  go look at the Programming Use Cases course, which goes into just building some projects and
       getting your hands dirty

---------------------------------------------------
